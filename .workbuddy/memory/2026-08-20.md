# 2026-08-20

## 14:41 增量编译复跑（用户要求"帮我编译"）
- 用 Ant 同款环境（PATH 注入 `C:\cygwin64\bin;C:\cygwin64\usr\bin;C:\WINDOWS\system32;C:\WINDOWS`，空 CLASSPATH）跑 `make images`：**exit=0，17m41s**，`Finished building target 'images'`，java -version = 28-internal。
- 全程含 jmod 打包 + CDS 归档，无失败日志，产物 `images/jdk` 完整可用。

## OpenJDK 28 原生 Windows 构建 — 全链路完成

### 已完成
1. **make images 成功**：`build/windows-x86_64-server-release/images/jdk/bin/java.exe` 可用，`openjdk version "28-internal" 2027-03-23`。
2. **Apache Ant 1.10.17** 下载到 `.workbuddy/apache-ant-1.10.17/`（用 PowerShell `Invoke-WebRequest` 下载，Cygwin `unzip` 解压）。
3. **`bin/idea.sh` 成功**（exit 0，17 秒）：生成官方 IDEA 工程。
   - `.idea/ant.xml` — Ant 构建面板，指向 `make/ide/idea/jdk/build.xml`，`build.target.dir = build/windows-x86_64-server-release`
   - `.idea/jdk.iml` — 官方模块文件（含全部 source roots + `inheritedJdk`）
   - `.idea/modules.xml` — 模块注册（`$PROJECT_DIR$/.idea/jdk.iml`）
   - `.idea/misc.xml` — `ProjectRootManager` project-jdk-type=JavaSDK（需手动设 SDK）
   - `.idea/env.cfg` — MODULE_ROOTS/MODULE_NAMES/BOOT_JDK/SPEC/CYGPATH
   - `.idea/classes/idea/` — IdeaLoggerWrapper 编译产物
4. **`build-jdk-windows.sh` 更新**：第 3 步加 `ANT_HOME` 自动探测（`export ANT_HOME="$(cd .workbuddy/apache-ant-* && pwd)"`）。

### 根因回顾（BASH_ENV/rm shim）
- WorkBuddy 的 `BASH_ENV` 注入把 `rm` 重定义为 shell 函数 → 调用 Cygwin 下崩溃的 `safe-bin/rm` → configure 误报 C compiler cannot create executables / config.status 无法创建 spec.gmk。
- 修复：`unset BASH_ENV; unset -f rm` + PATH 剔除 safe-bin/app.asar。已写入 `build-jdk-windows.sh` 与 `run-cygwin-build.sh`。

### Project Structure 打不开的两次排障（重要经验）
1. **第一次（缓存/Maven 冲突）**：IDEA 项目缓存里残留 Maven 追踪器，自动导入 `src/utils/IdealGraphVisualizer/` 下 20+ 个 pom.xml，`InvalidPathException: Illegal char <:>` 刷屏。修复：杀 IDEA → 删 `C:\Users\EDY\AppData\Local\JetBrains\IntelliJIdea2026.1\projects\jdk.3a207262\` 缓存目录 + 删 `.idea/workspace.xml` → 重启。
2. **第二次（真正的根因）**：`idea.sh` 模板生成的 `.idea/misc.xml` 里 `languageLevel="JDK_X"` 是占位符，IDEA 2026.1 不认识 → `LanguageLevel.getNonPreviewLevel` 抛 `Required value was null` → Project Structure 对话框模型崩溃（日志特征：`Last Action: ShowProjectStructureSettings`）。修复：改成 `languageLevel="JDK_25"`（配合已注册的 Temurin 25 SDK），杀 IDEA 重启后日志零 SEVERE（除 Grazie 网络错误，无害）。
   - **教训：以后凡是 idea.sh 生成的工程，必须把 misc.xml 的 JDK_X 占位符替换掉再打开。**


### 用户在 IDEA 中的操作步骤
1. 重新打开/Reload 项目（`D:\project\jdk`），IDEA 会加载新生成的 `.idea` 官方工程。
2. File → Project Structure → SDK → Add JDK → 选 `D:\project\jdk\build\windows-x86_64-server-release\images\jdk`。
3. View → Tool Windows → **Ant**（不是"Ant Build"）→ `make/ide/idea/jdk/build.xml` 已注册 → 双击 "images" 目标即可在 IDEA 内驱动 make 编译。

### Ant 插件安装（IDEA 2020.1+ 不再内置 Ant 支持）
- 插件市场 xmlId=`AntSupport`，数字 ID=23025，名为 "Ant"。
- 用户 IDEA build=IU-261.22158.277，兼容版本 `ant-261.22158.182`（since 261.22158 / until 261.*）。
- **下载 URL 模式**：`https://plugins.jetbrains.com/files/{pluginId}/{updateId}/{filename}.zip`（`/plugin/files/...` 会 404）。
- 静默安装：解压 zip 到 `C:\Users\EDY\AppData\Roaming\JetBrains\IntelliJIdea2026.1\plugins\`（zip 顶层自带 `ant/` 目录），重启 IDEA 即加载。
- 验证日志：`Loaded custom plugins: ... Ant (261.22158.182)` + `AntConfiguration: initialized` + build.xml 被 ant-imports 索引。

### Ant 面板双击 images 报 CreateProcess error=2（找不到 make）
- 报错：`make\ide\idea\jdk\build.xml:12: Cannot run program "make" ... CreateProcess error=2`。
- 根因两层：
  1. IDEA 的 Ant 是**原生 Windows 进程**，PATH 里没有 Cygwin → `make` 按名字解析不到。
  2. 即使用绝对路径 `C:/cygwin64/bin/make.exe`，原生进程拉起 Cygwin make 时**不会自动获得含 /usr/bin 的 PATH** → Makefile 里裸调 `rm`/`bash` 报 `make: rm: No such file or directory`（用 `make -q images` 复现）。
- 修复（改 `make/ide/idea/jdk/build.xml` 的 `call-make` 宏）：
  1. `executable="C:/cygwin64/bin/make.exe"`（不能用 Git Bash 的 make，不兼容本构建）。
  2. 加 `<env key="PATH" value="C:\cygwin64\bin;C:\cygwin64\usr\bin;C:\WINDOWS\system32;C:\WINDOWS"/>`。
  3. 覆盖 PATH 安全：spec.gmk 中 CC/CXX/LD/MT/RC 全是绝对路径（fixpath exec 前缀），不依赖 PATH 找 VS 工具链。
- 验证：PowerShell 模拟同款环境跑 `make -q images` → exit=1（`-q` 正常语义），stderr 零输出，rm 报错消失；与 Cygwin 基线一致。
- 坑：PowerShell `>` 重定向写 UTF-16，需 `iconv -f UTF-16LE` 解码查看；且 PowerShell 工具不回显 Cygwin 程序 stdout，落盘验证。

### Ant images 第二次报错：rm Permission denied / Device or resource busy（自锁）
- 报错：`exec returned: 2`，failure-summary.log 显示 `jlink_jdk_pre` 删除旧 `images/jdk` 时 `java.exe/jvm.dll/lib\modules` 被占用。
- 根因：**项目 SDK 被设成了 `images/jdk`**，IDEA 编译/索引进程启动该目录下的 java.exe → 把 make 要删的产物锁死（自锁）。
- 验证：当时无 java 进程后，用 Ant 同款环境（PATH 注入 + CLASSPATH="）重跑 `make images` → 2m52s 后 exit=0，`Finished building target 'images'`（含全部 CDS 归档）。整条 Ant→make→jlink 链路验证通过。
- **给用户的规约：Project SDK 不要指向 `build/.../images/jdk`（改用 boot JDK 或 Temurin 25），跑 Ant images 前确保没有进程在用 images/jdk。**

### Ant images 第三次报错：UnsupportedClassVersionError（IdeaLoggerWrapper class 71）
- 报错：`build.xml:4 taskdef idea.IdeaLoggerWrapper ... class file version 71.0, this version ... up to 69.0`。
- 根因：`idea.sh` 用 boot JDK 27 编译 `.idea/classes` 下的 IdeaLoggerWrapper（class 71），而项目 SDK 换成 Temurin 25 后，IDEA Ant 面板跟随项目 SDK 用 JDK 25 跑（只认 ≤69）。
- 修复：
  1. 用 boot JDK 27 的 javac `--release 25` 重编 `D:\project\jdk\.idea\src\idea\IdeaLoggerWrapper.java` 覆盖 `.idea/classes`（javap 验证 major=69）。
  2. `bin/idea.sh` 第 268 行 javac 命令加 `--release 25`，防以后重跑 idea.sh 回退。

### Ant images 第四次报错：exec returned: 2（SDK 切换瞬间的自锁）
- 现象：`build.xml:17 exec returned: 2`，但 failure-logs 为空（10:33 被清空后无新日志）、failure-summary.log 仍是 10:05 旧文件 —— 说明是 make bootstrap 期失败，非 recipe 级失败，与 10:04 那次 jlink 锁不同。
- 证据链：
  1. IDEA 日志 10:33:46 `JpsGlobalModelSynchronizerImpl - Saving global entities ... SdkEntity` —— 用户**正在 Project Structure 改 SDK**（换成 Temurin 25）。
  2. 无任何 java 进程残留；images/jdk 产物时间戳 10:09（未被改动）。
  3. Ant 同款环境（PATH 注入 + CLASSPATH=""）重跑 `make images` → 2m18s exit=0，`Finished building target 'images'`。
- 结论：10:33 的 Ant 很可能是**在 SDK 切换生效前的旧配置下启动**（Ant JVM 仍跑在 images/jdk「28」上 → 自锁自己 JVM 的文件，make 删除失败退出）。SDK 保存为「25」(Temurin 25) 后即自愈。
- 教训：**改完 Project Structure 的 SDK 后要等状态保存完再跑 Ant**；若再报 exec returned 2，先看 Ant 面板 Build File Properties → Execution 的 JDK 是否为项目 SDK。

### 关键路径
- 源码：`D:\project\jdk`（JDK 28 主线）
- 产物：`D:\project\jdk\build\windows-x86_64-server-release\images\jdk`
- boot JDK：`C:\Users\EDY\.jdks\openjdk-ea-27-ea+34-2321`
- Ant：`D:\project\jdk\.workbuddy\apache-ant-1.10.17`
- 构建脚本：`bash .workbuddy/run-cygwin-build.sh`（一键：configure → make images → idea.sh）

## 15:24 源码跟读记录输出（用户："输出一份到源码记录目录"）
- 新建 `D:\project\jdk\.workbuddy\docs\` 作为源码记录目录（README.md 索引 + 01-hashCode-identity-hash.md）。
- 文档结构：快速概览 + 核心文件索引表 + TOC + 编号小节（入口→两条路径→FastHashCode→六种算法→mark word→JIT intrinsic→验证实验→全链路 SVG 图）+ 附录索引。配色蓝/紫/橙/绿。
- **行号全部重新 grep 核实**（教训：口头讲过的行号可能记错——jvm.cpp 入口实为 787，FastHashCode 调用在 836；之前口头说 836 是入口是错的）：
  - Object.java:130-131；jvm.cpp:787(JVM_IHashCode)/836(FastHashCode 调用)；synchronizer.cpp:678-700(FastHashCode)、638-676(get_next_hash 六算法)；markWord.hpp:141/151/165/188/293-298；oop.cpp:118-122；oop.inline.hpp:425；library_call.cpp:252；c2compiler.cpp:620；globals.hpp:294(InlineObjectHash)；vmIntrinsics.cpp:469。
- 待跟进主题（README 里登记）：String.intern、锁升级、双亲委派、G1 GC。

## 15:40 hashCode 交互动画固化（用户："重要的是交互动画"）
- 新增 `D:\project\jdk\.workbuddy\docs\01-hashCode-markword-animation.html`：自包含 HTML（零外部依赖），8 步逐步播放 mark word 安装 identity hash 全过程。
- 步骤：①对象诞生 → ②调用 hashCode（Object.java:130-131）→ ③JNI 入口（jvm.cpp:787/836）→ ④读对象头（synchronizer.cpp:685）→ ⑤检查 hash==0（687-690）→ ⑥xor-shift 生成 0x2F1A8C3D（655-673）→ ⑦CAS 安装（692-697）→ ⑧再次调用命中（687-689）。
- 控件：Space 播放/暂停、←→ 步进、R 重置、步骤胶囊导航；代码面板高亮当前执行行。
- **JDK 28 mark word 布局（重要，与旧资料不同）**：`unused:22 | hash:31 | valhalla:4 | age:4 | self-fwd:1 | lock:2`（markWord.hpp:48-51）——**已无 biased_lock 位**（valhalla 取代），hash 为 31 位（旧版是 25 位 + biased 1 位）。
- README.md 新增"交互动画"小节，01 主文档快速概览后加配套动画引用。

## Object 系列全量跟读完成（docs/object/ 新建，用户："把 Object 相关的都读了，docs 下新建文件夹"）
- 新建 `D:\project\jdk\.workbuddy\docs\object\`：README.md（系列索引：为什么先读 Object、7 篇目录表、阅读模板、待跟进主题）+ 01~07 七篇 + 1 个动画 HTML。
- 结构：01-object-overview（13 方法全景表 + JDK 28 类注释 Valhalla 预告 + native 绑定机制演进 + 全链路 SVG）、02-getClass、03-hashCode（迁移改名+修正）、03-hashCode-markword-animation.html（从 top-level 迁移）、04-clone、05-equals-toString、06-wait-notify、07-finalize。顶层 `docs/README.md` 已重写指向 object 系列。
- **JDK 28 native 绑定机制重大发现（与旧资料不同）**：
  - Object 已无 `registerNatives()`（JDK 8 时代有）；5 个 native（hashCode/wait0/notify/notifyAll/clone）由 VM 静态注册：`javaClasses.cpp:92-104` `java_lang_Object::register_natives`，调用点 `vmClasses.cpp:172`。
  - `wait_name` 的符号值是 `"wait0"`（vmSymbols.hpp:462）——wait0 是 private final（Object.java:471，"final modifier so method not in vtable"）。
  - `getClass` 是例外：走标准 JNI 名字解析（Object.c:43 → `jni_GetObjectClass` jni.cpp:1028-1037 → `Klass::java_mirror()` klass.inline.hpp:96-98）。
  - 实证命令：`-Xlog:jni+resolve=debug`（注意 tag 是 `jni+resolve`，`jni+load` 不存在）。
- 各篇关键锚点：JVM_IHashCode jvm.cpp:787（valhalla 分支 793-822、FastHashCode 调用 836）；JVM_Clone jvm.cpp:859-914（Cloneable 检查 875-883、value 分支 885-889）；ObjectSynchronizer::wait synchronizer.cpp:547-566（545 注释 "must use heavy weight monitor to handle wait()"）、notify 快速路径 582-585；ObjectMonitor::wait objectMonitor.cpp:1657/notify 2032/notifyAll 2060；wait(long) 虚拟线程分支 Object.java:458-465；`--finalization=` 开关 arguments.cpp:2451-2458（instanceKlass.cpp:204 默认 enabled）。
- **JDK 28 相对旧资料的差异清单（写入文档）**：mark word 无 biased_lock 位（valhalla:4 取代，hash 31 位）；wait0 改名；Object 无 registerNatives；Valhalla（inline class）已渗透 hashCode/clone/getClass 语义。
- 待跟进（系列 README 登记）：synchronized 锁升级下钻 ObjectMonitor、String.intern、双亲委派、G1 GC、Java mirror 下钻 javaClasses.cpp、Valhalla value class 模型。

## Object 系列交互动画补齐（用户："重要的是要有交互动画"）
- 新增 2 个自包含 HTML 动画（沿用 hashCode 动画风格：蓝紫橙绿、Space/←→/R、步骤胶囊、真实源码深色面板、四色解说 tag）：
  1. `docs/object/06-wait-notify-monitor-animation.html`：锁膨胀 + ObjectMonitor 队列流转 12 步。T1 wait 强制膨胀（fast-locked→monitor，synchronizer.cpp:545 注释）、T1 挂入 _WaitSet + park（objectMonitor.cpp:1657）、T2 notify 快速路径检查（synchronizer.cpp:582-585）、WaitSet→EntryList 搬移（objectMonitor.cpp:2032）、T1 重竞争持锁。含 Owner/cxq/_EntryList/_WaitSet 四区实时状态 + 线程泳道。
  2. `docs/object/04-clone-shallowcopy-animation.html`：clone 浅拷贝 9 步。Cloneable 检查（jvm.cpp:875-883）→ value class 分支（885-889）→ 分配（891-902）→ HeapAccess::clone 逐字段（id 值拷贝 + arr 引用照抄共享同一数组）→ finalizer 检查（904-911）。直观展示 c.arr==d.arr → true。
- 04/06 两篇文档头部加"配套交互动画"引用小节；README 系列目录表 04/06 行挂动画链接 + 新增"交互动画"汇总小节（3 个动画）。
- 至此 Object 系列动画覆盖 3 个：mark word 安装 hash（03）、clone 浅拷贝（04）、wait/notify 锁膨胀（06）。

## 关键字系列启动（docs/keywords/ 新建，用户："比 object 更关键的是 java 的关键字"）
- 用户选择"全景总览先行"→ 新建 `D:\project\jdk\.workbuddy\docs\keywords\`：README.md（系列索引+阅读方法+待跟进）+ 01-keywords-overview.md + 01-keywords-overview.svg（四色手绘分类地图）。
- 01 全景篇核心：JLS §3.9 的 50 关键字按"背后机制"分 7 域（并发/对象内存/生命周期/异常/控制流/声明/字面量保留，橙绿蓝紫四色）；**三类本质**：机制型（读 HotSpot）/ 编译期型（读 javac）/ 语义型（读 JLS，判断法=class 文件搜不搜得到痕迹）；关键字→机制映射表（真实行号）。
- 深读优先级：synchronized → volatile → new → static → instanceof → try/catch → final（P0/P1/P2，登记在 README）。
- **新验证的锚点（grep 实证，非记忆）**：
  - synchronized：`bytecodes.hpp:238`（_monitorenter=0xc2）、`interpreterRuntime.cpp:783`（慢路径入口）、`templateTable_x86.cpp:4045/4154`（x86 模板，注意在 cpu/x86 不在 share）
  - volatile：`classFileParser.cpp:4626`（ACC_VOLATILE 解析）、`orderAccess.hpp:116-126`（fence 定义表）、`memnode.hpp:1376`（MemBarVolatileNode，基类 1227）
  - new：`threadLocalAllocBuffer.inline.hpp:38`（TLAB allocate 快路径）、`memAllocator.hpp:81/46/50/53`（allocate/TLAB fast/slow/outside）
  - instanceof：`templateTable_x86.cpp:3934`、`klass.cpp:141`（is_subclass_of）、`klass.inline.hpp:121`（is_subtype_of）、`library_call.cpp:4519`（C2 gen_instanceof）
  - static：`instanceKlass.cpp:1417`（initialize_impl，JDK 28 方法名，入口 952/963）
  - try/catch：`constMethod.hpp:111`（ExceptionTableElement）、`method.hpp:998`（ExceptionTable 助手类）、`method.hpp:293`（exception_table_start）、athrow `templateTable_x86.cpp:4022`
  - final：`classFileParser.cpp:880/1273/6444`（ConstantValue 属性）
- 顶层 `docs/README.md` 目录表加 keywords 系列 + 速览表；synchronized 待跟进项勾除（已并入关键字主线）。

## synchronized 锁升级全链路深读（docs/keywords/02，用户："从 synchronized 锁升级全链路开读"）
- 新建 `docs/keywords/02-synchronized-lockupgrade.md`（9 节：字节码 → 解释器模板 → fast_lock 内联 CAS → 四段式 enter → 膨胀 → ObjectMonitor 结构 → 释放唤醒 → JDK 28 vs 教科书差异表 → 验证实验）+ `02-synchronized-lockupgrade.svg`（四色状态机：unlocked 01 / fast-locked 00 / monitor 10 + 四段式步骤带 + ObjectMonitor 运行时快照）。
- **JDK 28 锁实现重大发现（全部 grep 实证，与教科书/旧资料差异巨大）**：
  1. **偏向锁代码整体删除**：`UseBiasedLocking` 全仓 grep 零命中；mark word `11` 位是 GC marked（markWord.hpp:58），旧资料里是 biased。
  2. **轻量锁不再用 displaced header**：mark word 锁位直接 CAS `01→00`（macroAssembler_x86.cpp:10626-10636），**不写任何指针**；"谁持有锁"由线程 **LockStack** 追踪（push 10641-10645），递归=栈顶比较 O(1)（10619-10620）。
  3. **ObjectMonitor 结构大改**（objectMonitor.hpp:172-200）：`_owner` 是 `int64_t owner_id`（不再是指针）；**`_cxq` 已删除**，只剩 `_entry_list`（双向链表 + 新增 `_entry_list_tail`）+ `_wait_set`；新增 `_unmounted_vthreads`（虚拟线程计数）。
  4. **膨胀前必须先 FastHashCode 装 hash**（synchronizer.cpp:1954-1955，注释 "Lightweight monitors require that hash codes are installed first"）——identity hash 与 monitor 指针都住 mark word。
  5. **enter 四段式**（synchronizer.cpp:1725-1783）：① try_recursive_enter 递归 ② fast_lock_try_enter ③ fast_lock_spin_enter 自旋（短临界区不膨胀）④ inflate_and_enter 膨胀兜底；全程处理 async deflation 竞争。
  6. **解释器内联**：templateTable_x86.cpp:4045（monitorenter，含 inline type 检查 4051-4053 → throw_identity_exception 4149）→ interp_masm_x86.cpp:1167 lock_object → macroAssembler_x86.cpp:10589 fast_lock；慢路径 interpreterRuntime.cpp:783 → enter。
  7. **释放唤醒**：exit（synchronizer.cpp:1785）fast-locked 走 CAS+LockStack pop / monitor 走 monitor->exit（objectMonitor.cpp:1401）→ exit_epilog（1519）选 _succ 只唤醒一个避免惊群。
- 新增 `docs/keywords/02-synchronized-lockupgrade-animation.html`：12 步交互动画（T1 无锁→轻量锁 CAS+LockStack push→T2 CAS 失败→自旋→膨胀装 hash→00→10→T2 入 entry_list park→T1 exit→唤醒 T2 交棒），含 mark word 锁位放大、双线程泳道、LockStack/ObjectMonitor 双视图，沿用系列风格。
- 系列 README 02 行 ✅ + 动画链接；顶层 docs/README.md 目录表/速览表/交互动画表同步更新。
- 锁升级新认知链：Object 06 的 wait/notify（monitor 态操作）↔ 本系列 02（如何进 monitor 态）闭环；`_entry_list` 排队细节（EnterI/exit_epilog）留待后续下钻。

## volatile 内存屏障深读（docs/keywords/03，用户："继续"）
- 新建 `docs/keywords/03-volatile.md`（9 节：JMM 语义 → 字节码无痕 → C2 识别与插屏障 → x86 机器码真相 → OrderAccess API → happens-before 与四类重排 → DCL 构造器屏障 → volatile vs synchronized → 验证实验）+ `03-volatile-membar.svg`（四类重排矩阵 + happens-before 双线程时序图）。
- **volatile 全链路锚点（grep 实证）**：
  1. 唯一识别入口：`classFileParser.cpp:4626`（`ACC_VOLATILE` 位）→ 无专属字节码（getfield/putfield + 标志位）。
  2. C2 解析：parse3.cpp:259（`is_vol = field->is_volatile()`）→ 318（`decorators |= is_vol ? MO_SEQ_CST : MO_UNORDERED`）→ 320（access_store_at）；读侧 198。
  3. 插屏障 `BarrierSetC2::C2AccessFence`（barrierSetC2.cpp）：写前 `Op_MemBarRelease`（280-287）；读后 `Op_MemBarAcquire` + set_trailing_load（353-360）；写后 `Op_MemBarVolatile`（343-352，仅非多拷贝原子 CPU）；`mem_node_mo()`（377-389）volatile 写→release 读→acquire。
  4. **x86 机器码真相**：`MemBarAcquire`/`MemBarRelease` **空编码**（x86.ad:8855-8899，ins_cost(0) size(0)）；`MemBarVolatile` = `lock addl [rsp],0`（x86.ad:8916-8929）；还有 `unnecessary_membar_volatile` 优化（8931-8941）→ x86 上 volatile 读零额外指令、写仅 1 条 lock addl。
  5. OrderAccess 运行时 API（orderAccess.hpp:238-248）；windows_x86 实现（orderAccess_windows_x86.hpp:42-53）前三个零指令、storeload=fence；fence stub（stubGenerator_x86_64.cpp:584-602）= `membar(StoreLoad)`；汇编展开（assembler_x86.cpp:208-242）`lock; addl(rsp,offset),0`，offset 取 -L1_line_size 防栈冲突。
  6. DCL 构造器收尾：parse1.cpp:1069-1114 `do_exits`，final/stable/volatile 写后插 `Op_MemBarStoreStore`/`Op_MemBarRelease`（UseStoreStoreForCtor 可选）。
- 新增 `docs/keywords/03-volatile-membar-animation.html`：12 步交互动画（无 volatile 对照版 StoreLoad 重排丢消息 ❌ → 加 volatile → C2 插三屏障 → lock addl 排空 Store Buffer → happens-before 建立 y=1 ✅ → x86 成本对比），含共享内存视图 + Store Buffer 动态 + 双线程泳道 + 真实源码面板。
- 系列 README 03 行 ✅ + 动画链接；顶层 docs/README.md 目录表（✅ 03 篇）/速览表/交互动画表同步更新。
- 衔接关系：02（synchronized 靠 monitor）↔ 03（volatile 靠屏障）都是"机制型"关键字；volatile 的 C2 屏障插入规则（C2AccessFence）与 02 的 fast_lock 互不重叠，属于 JMM 侧主线。

## new / TLAB 对象诞生深读（docs/keywords/04，用户："不要问，继续"）
- 新建 `docs/keywords/04-new.md`（10 节：字节码 → 解释器四连查 → TLAB bump-pointer → 慢路径三段式 → 对象头初始化 → 构造器 → C2 消灭分配 → JDK 28 差异表 → 验证实验 → 与 02/03 闭环）+ `04-new-tlab.svg`（全链路流程图：字节码 → 快路径 → 慢路径分支 → 对象头 → 构造器 + C2 彩蛋 + 对象布局图）。
- **new 全链路锚点（grep 实证）**：
  1. `bytecodes.hpp:231`（_new=187 0xbb）→ `templateTable_x86.cpp:3732`（TemplateTable::_new）：CP tag 检查 3746 → load_resolved_klass_at_index 3750 → **clinit_barrier 3756（static 的渗透点）** → layout_helper 3759 + slow_path_bit 3761 → tlab_allocate 3774。
  2. **TLAB bump-pointer 指令展开**（barrierSetAssembler_x86.cpp:281-313）：mov obj=top 296 → lea end 298-301 → cmp end vs end 302 → mov top=end 306；线程私有 r15_thread，零锁零原子。
  3. C++ 侧 `threadLocalAllocBuffer.inline.hpp:38-52`（allocate）；ResizeTLAB 动态放大（57-68，refill 多自动翻倍最多 16×）。
  4. **慢路径三段式**（memAllocator.cpp:327-348）：① inside_tlab_fast（249）→ ② inside_tlab_slow refill（253-325，核心权衡 276 free>waste → 堆外 / 否则 retire_tlab 287 + compute_size 290 + allocate_new_tlab 299 + fill_tlab 322）→ ③ outside_tlab（235-247 大对象直接堆分配）。
  5. 慢路径入口 `interpreterRuntime.cpp:223`（JRT_ENTRY _new：klass_at → check_valid_for_instantiation → initialize_preemptable → allocate_instance）；`instanceKlass.cpp:1936`（allocate_instance → obj_allocate）；`collectedHeap.inline.hpp:36`（obj_allocate → ObjAllocator）。
  6. 对象头初始化（templateTable_x86.cpp:3792-3830）：8B 清零循环 3807-3813 → mark=prototype 3823（unlocked 01，锁升级起点）→ klass gap/klass 3829-3830；UseCompactObjectHeaders 合成单 64 位头 3818。
  7. 构造器 `invokespecial <init>`（templateTable_x86.cpp:3496）。
  8. C2：AllocateNode（callnode.hpp:1112）/AllocateArrayNode（1234）→ expand_allocate（macro.cpp:2413）→ **标量替换** EliminateAllocations=true（c2_globals.hpp:584）+ escape.cpp:416；未逃逸对象 new 被整体删除，锁消除联动。
- 新增 `docs/keywords/04-new-tlab-animation.html`：12 步交互动画（字节码 → 四连查 → bump-pointer 指针前移动画 → 对象头 → 构造器 → TLAB 不够进慢路径 → 剩余空间决策面板 free vs waste → refill 换新 TLAB → 大对象 Humongous → 汇合 → C2 标量替换消灭分配），含 Eden/TLAB/大对象三区堆视图 + top/end 指针 + 决策 gauge，沿用系列风格。
- 系列 README 04 行 ✅ + 动画链接 + 待跟进勾除；顶层 docs/README.md 目录表（✅ 04 篇）/速览表/交互动画表同步更新。
- 四关键字闭环：new（对象出生，mark=prototype unlocked）→ synchronized（第一次加锁演升级）→ volatile/final（构造器出口屏障保发布安全）→ static（clinit 屏障先于 new）。下一篇候选：static（instanceKlass.cpp:1417 initialize_impl）。

## static / 类初始化 &lt;clinit&gt; 状态机深读（docs/keywords/05，用户："继续"）
- 新建 `docs/keywords/05-static.md`（11 节：static 三兄弟 → 字节码视角无专属字节码 → ACC_STATIC 解析 → 触发条件 JLS 12.4.1 → 六态状态机 → initialize_impl 十一小步 → mirror 初始化锁 → clinit_barrier 两跳快路径 → JDK 28 新发现 → 验证实验 → 系列闭环）+ `05-static-clinit.svg`（四色状态机：linked → being_initialized → fully_initialized/initialization_error + clinit 执行细节带 + 并发场景三卡片）。
- **static 全链路锚点（grep 实证）**：
  1. 类文件侧：`ACC_STATIC`（classFileParser.cpp:2289-2291 字段清理、2317 方法 args_size=0）；`<clinit>` 符号（vmSymbols.hpp:397）。
  2. 六态枚举（instanceKlass.hpp:195-202）：allocated → loaded → linked → being_initialized → fully_initialized → initialization_error；`_init_state`（274，load_acquire 读）/`_init_thread`（286，递归重入检测 `is_reentrant_initialization` 618）。
  3. 入口：`InstanceKlass::initialize`（961-966）→ `initialize_impl`（1417）。**十一小步**：Step1 抢 mirror 初始化锁（1435-1436）→ Step2 while being_initialized && !reentrant → wait_uninterruptibly（1442-1451，JFR 事件 ThreadWaitingForClassInit）→ Step3 递归重入直接 return（1454-1462）→ Step4 已完成 return（1465）→ Step5 error 态抛 NoClassDefFoundError（1476-1494）→ Step6 set being_initialized + _init_thread（1498-1499）→ Step7 父类递归初始化 + 默认方法接口（1536-1547）→ Step8 call_class_initializer（1578）→ Step9 set fully_initialized + notify_all（1620）→ Step10/11 异常→initialization_error→ExceptionInInitializerError（1624-1648）。
  4. **初始化锁住在 Java mirror 里**（instanceKlass.cpp:910-918 `java_lang_Class::init_lock(java_mirror())`，fully_initialized 后 fence_and_clear_init_lock 清空可 GC）；set_initialization_state_and_notify（1654-1667）先清 _init_thread 再置状态再 notify_all，顺序敏感。
  5. call_class_initializer（2019-2059）：CDS 快路径（AOTClassInitializer::call_runtime_setup 2030 / CDSEnumKlass 2034）+ JavaCalls::call 无参静态调用（2057）；class_initializer()（2010-2017）find_method("<clinit>", "()V")。
  6. clinit_barrier 宏（macroAssembler_x86.cpp:4835-4860）：两跳快速路径各 2 条指令（① init_state==fully_initialized ② r15_thread==_init_thread 递归放行），miss 走解析慢路径；插入点：invokestatic（templateTable_x86.cpp:2327-2336）、getstatic/putstatic（2376-2382）、new（3756）；x86 TSO 下 acquire 天然满足（4846 注释）。
  7. **JDK 28 新特性 strict static fields**（instanceKlass.cpp:1587-1615）：`fields_status` 1 字节/字段位数组（classFileParser.cpp:5917 post_process_parsed_stream 建立），putstatic 时清位（notify_strict_static_access 1669），&lt;clinit&gt; 后未赋值 → throw_strict_static_exception；classFileParser.cpp:1553/5547。
  8. Valhalla 渗透：inline klass nullable 布局预分配全零 null-reset 值（1513-1531）；初始化线程 NoPreemptMark 防虚拟线程卸载（1510）。
- 新增 `docs/keywords/05-static-clinit-animation.html`：12 步交互动画（T1 首触 clinit_barrier 快路径 miss → 抢 mirror 锁 → Step6 变橙 → 父类跳过 → 执行 &lt;clinit&gt; 字节码逐行高亮 + strict 位逐个清零 → T2 并发触达挂起（紫色 wait 泳道）→ strict 校验通过 → Step9 notify_all 变绿 + 锁清空 → T2 醒来直接放行读到 42 → 错误路径回顾），含六态徽章 / mirror 锁 / strict 位数组 / 双线程泳道 / &lt;clinit&gt; 字节码面板 / instanceKlass.cpp 源码面板，沿用系列风格。
- 系列 README 05 行 ✅ + 动画链接 + 待跟进勾除（死锁问题已在 05 §7 用 _init_thread 递归检测回答）；顶层 docs/README.md 目录表（✅ 05 篇）/速览表/交互动画表同步更新。
- 衔接链更新：04 的 new 模板 clinit_barrier（templateTable_x86.cpp:3756）= static 渗透点；初始化锁本质是 synchronized（ObjectLocker + mirror 隐藏字段），与 02 锁体系闭环。下一篇候选：instanceof（klass.cpp:141 is_subclass_of 已有锚点）。

### 06-08 三篇连推（"直达把关键字都完成"批次 1）

- **06 instanceof**（`docs/keywords/06-instanceof.md` + `.svg` + `-animation.html`）：
  1. 字节码 `_instanceof=0xc1`（bytecodes.hpp:237），`atos→itos` 栈顶消耗出 0/1（templateTable_x86.cpp:3934）。
  2. 快路径 `check_klass_subtype`（macroAssembler_x86.cpp:4047）：self-check → 显示表 O(1)（`super_check_offset` 分派）→ 慢路径 `search_secondary_supers`；`gen_subtype_check` 在 interp_masm_x86.cpp:586（不在 macroAssembler！）。
  3. 接口检查：`_secondary_supers_bitmap` + `_hash_slot` 位图哈希（SECONDARY_SUPERS_TABLE_SIZE 64）。
  4. C2 `gen_instanceof`（graphKit.cpp:3576）三路 merge（null/成功/失败），可静态折叠/画像收窄；`checkcast`（templateTable_x86.cpp:3879）失败抛异常且对象原样保留（atos→atos）。
- **07 异常全家桶**（`docs/keywords/07-exceptions.md` + 配套）：try/catch/finally/throw/throws 五关键字：
  1. javac 摊平异常表 `ExceptionTableElement` 4×u2（constMethod.hpp:111），`catch_type_index==0` 是 catch-all 免类型比较。
  2. 查找 `fast_exception_handler_bci_for`（method.cpp:275）：区间覆盖 [beg,end) → catch-all 短路 → `ex_klass->is_subtype_of(k)`（复用 06 的 instanceof 算法）。
  3. `athrow`（templateTable_x86.cpp:4022）null_check 把 throw null 转 NPE；无 handler 走 `remove_activation_entry` 解栈上抛（interpreterRuntime.cpp:507）。
  4. finally 无字节码、javac 复制到每个退出点；`throws` 编译成 `Exceptions` 属性（classFileParser.cpp:1862 `parse_checked_exceptions`），运行期零作用。
- **08 final 三面孔**（`docs/keywords/08-final.md` + 配套）：
  1. static final → `ConstantValue` 属性（仅 is_static，classFileParser.cpp:1270）+ javac 常量折叠；JDK 28 strict static 对带 ConstantValue 字段自动豁免（instanceKlass.cpp:6444 附近）。
  2. final 方法不进 vtable（Object.java:471 wait0 注释实证），C2 直接内联。
  3. final 实例字段 JMM freeze：`Parse::do_exits`（parse1.cpp:1069-1114）构造器出口 MemBarStoreStore/MemBarRelease，x86 空编码，异常路径不插。
- 跨篇闭环：① catch 类型匹配 = instanceof 算法复用；② strict static 豁免 = 05 与 08 交互；③ 06/07 共用 `is_subtype_of`。
- 系列 README 06/07/08 行 ✅ + 动画链接 + 待跟进三项勾除；顶层 docs/README.md（✅ 08 篇）同步。
- 下一批：09 控制流+字面量+编译期型补完篇（if/else/switch/tableswitch 0xaa/lookupswitch 0xab/for/while/do/break/continue/return/yield、true/false/null/void/var/assert/strictfp/_）；10 声明域+访问控制+module 补完篇（class/interface/enum/record/extends/implements/abstract/sealed/permits/public/protected/private/package/import/transient + module 系 + module-info.class 解析）。

### 09-10 收官两篇（"直达把关键字都完成"批次 2 → 系列完结）

- **09 控制流+字面量+编译期型**（`docs/keywords/09-control-flow-and-literals.md` + `.svg` + `-animation.html`）：
  1. if 家族 0x99-0xa6（bytecodes.hpp:197-210）映射到 4 个共享模板 `if_0cmp`/`if_icmp`/`if_acmp`/`if_nullcmp`（templateTable_x86.cpp:1918-1999），条件参数化实例化 6 条件；`if_acmp` 含 Valhalla 渗透（inline type substitutable 比较 :1960-1991）。
  2. for/while/do+break/continue = `goto` 0xa7；`goto`/`jsr` 共享 `branch(bool is_jsr, bool is_wide)`（templateTable_x86.cpp:1749），**回边计数**（:1806，仅向后分支）溢出 → OSR 栈上替换。
  3. **lookupswitch 改写**：模板直接 `__ stop("lookupswitch bytecode should have been rewritten")`（templateTable_x86.cpp:2070）；rewriter.cpp:410-427 类加载时原地改写为 `fast_linearswitch`（<5 对）或 `fast_binaryswitch`（≥5 对），阈值 `BinarySwitchThreshold=5`（globals.hpp:1579）。
  4. **javac switch 决策公式**（Gen.java:1380-1391）：`table_space+3*table_time <= lookup_space+3*lookup_time` → tableswitch : lookupswitch；tableswitch 4 字节对齐强制（Gen.java:1395 `code.align(4)`）。
  5. return 家族 6 条指令 0xac-0xb1 共享 `_return(state)` 模板（templateTable.cpp:407-412）：① safepoint poll（:2244）② `narrow(rax)` 收窄（:2258）③ `remove_activation` 撤帧；`return_register_finalizer` 彩蛋（:2227-2239）。
  6. 纯编译期型：`var`（受限类型名 Resolve.java:2605）、`_`（Tokens.java:159 保留字）、`yield`（受限标识符 JavacParser.java:250）、`assert`（Lower.java:3137 降级 `$assertionsDisabled + if + athrow`，合成字段 Lower.java:99/126-127）、`strictfp`（classFileParser.cpp:2291 ACC_STRICT 剥除，`_major_version <= JAVA_16_VERSION` 才保留）。
  7. 字面量：`true`→iconst_1 0x04、`false`→iconst_0 0x03、`null`→aconst_null 0x01、`void`→描述符 V（Tokens.java:146）。
- **10 声明域+访问控制+module 收官**（`docs/keywords/10-declaration-and-module.md` + `.svg` + `-animation.html`）：
  1. **access_flags 一本账**：三组识别掩码（jvm_constants.h:31-62 CLASS/FIELD/METHOD），类掩码无 PRIVATE/PROTECTED；**位复用陷阱**：0x0020 类=IDENTITY/方法=SYNCHRONIZED，0x0040 字段=VOLATILE/方法=BRIDGE，0x0080 字段=TRANSIENT/方法=VARARGS；**JVM_ACC_IDENTITY** 是 Valhalla 对老 ACC_SUPER 的重命名（jvm_constants.h:33）。
  2. 形态=标志组合（classFileParser.cpp:4507-4520）：interface=INTERFACE+ABSTRACT、enum=FINAL+ENUM、record=FINAL+RECORD、@interface=+ANNOTATION。
  3. `sealed/permits`：ACC_SEALED + PermittedSubclasses 属性（parse_classfile_permitted_subclasses_attribute :3233；vmSymbols.hpp:217）；双重校验：编译期 javac + 加载期 `has_as_permitted_subclass`（instanceKlass.cpp:282）。
  4. `record`：ACC_RECORD + Record 属性（parse_classfile_record_attribute :3322；vmSymbols.hpp:207），VM 只存元数据供反射。
  5. `extends/implements`：super_class 1 个 u2 + interfaces 数组（check_super_class :4005）；链接期 `initialize_supers`（:5663）构建继承链 → 06 篇 instanceof 的 super_check 数据结构。
  6. 访问控制"只有位没有兵"：VM 只做三互斥检查（has_illegal_visibility :4551-4559），运行期零强制；访问控制在 javac + 反射层。
  7. `package` 并入全限定名零字节码；`import` 全系列唯一"完全零痕迹"；`transient` 标志活着机制死了（accessFlags.hpp:57 只有位，序列化是 Java 层 ObjectStreamClass）。
  8. **最高价值发现：module-info.class 不在 HotSpot 解析**——ACC_MODULE 特殊类（classFileParser.cpp:4492-4503 当类加载抛 NoClassDefFoundError）；Module 属性由 java.base Java 层 `ModuleDescriptor.read()`/`jdk.internal.module.ModuleInfo` 解析 → `Modules.defineModule()` → VM 入口 `JVM_DefineModule`（jvm.cpp:1316）→ ModuleEntry（moduleEntry.cpp:263）；requires/exports/opens/uses/provides/with 是 JLS 受限关键字对应 Module 属性五张表。
- 顶层 docs/README.md：目录表（✅ 10 篇收官）+ 速览表补 09/10 + 交互动画表补 09/10；keywords/README.md 待跟进最后一项 module 解析勾除。
- **系列收官统计：50 关键字全部落位，10 篇主文档 + 8 个交互动画 + 8 张 SVG（01/06/07/08/09/10 均有 SVG；02-05 也各有 SVG）**。

## 17:17 修复 04-new-tlab-animation.html 三处 bug（用户质疑"是不是有问题"）
- **逻辑硬伤**：第 ⑦ 步 `top+size = 86% > end 92%` 数学上 86 不大于 92 却判"超限"——数值与结论矛盾。改为 top=70/end=92，需 40% → 110% > 92% 真正超限，setObj/setMarks 同步。
- **面板与堆视图脱节**：第 ⑧ decide 步加 `setMarks(46,92)` 使堆剩余=面板 free=46%；第 ⑨ refill 步 `setMarks(24,92)`→`(84,92)` 使堆剩余=面板 free=8%（原 68% 与 8% 矛盾）。
- **高亮灯指错行**：`bump`/`zero`/`slow1` 高亮索引原指向注释行/越界行（i=0/6/8/11），修正为指向真实关键代码（movptr top=end → i=5、mark=prototype → i=5、store_klass → i=6、outside_tlab → i=10）；c2 去掉注释行高亮（i=0→i=3/6）。
- 经验：交互动画里的"教学示意百分比"仍需内部自洽（文字结论、面板数字、堆图形三处必须一致），否则比真实字节码更易暴露破绽。

## 17:21 排查并修复交互动画的两个运行时 bug（用户："只有1步？下一步没反应"）
- **04-new-tlab-animation.html 致命语法错误**：第④步 navi 字符串里"落地"用了 **ASCII 双引号**，提前截断字符串，后续 `落地` 被当非法标识符 → 整个 `<script>` 解析失败 → JS 从未执行 → 代码面板空、胶囊无、按钮 onclick 未绑定 → 用户看到"只有1步、下一步没反应"的静态空壳。这是上一轮"是不是有问题"时修的 3 处逻辑（高亮行号/数值一致）**当时从未生效**的根因——脚本根本没跑。去掉引号后 SYNTAX_OK。
- **03-volatile-membar-animation.html 运行时错误**：`s.barRel.show()`/`s.barVol.show()`/`s.barAcq.show()` 共 7 处，原生 DOM 元素无 `.show()`（jQuery API），自包含 HTML 不引 jQuery → 步进到屏障可视化那几步抛 TypeError 崩溃。在 `<script>` 开头加 `Element.prototype.show/hide` polyfill 修复。
- **全量验证**：用 jsdom 把 8 个 `*-animation.html` 逐一加载并模拟点击"下一步"至末步、捕获 jsdomError。修复后**全部 clean**（02/05/06/07/08/09/10 本来即 clean；02 胶囊容器 id=pills / class=.step、03 用 .barrier，与 04 的 nextBtn/.cap 控制栏模板不一致，但各自能交互）。
- **两套控制栏模板并存**：04 用 `nextBtn`/`playBtn`/`caps`(`.cap`)；02/03/05/06/07/08/09/10 用 `btnNext`/`btnPlay`/`chips`(`.chip`/`.step`)。功能均正常，仅命名/类名不统一（如需统一可后续批量改）。
- 经验：交互动画交付前必须用 jsdom（或真实浏览器）做"加载 + 逐点步进 + 捕获运行时异常"的 **smoke test**，不能只靠肉眼和语法检查——语法 OK ≠ 运行时 OK，04 与 03 都是"走着走着卡死"类 bug（一个语法错、一个缺 API）。验证脚本在 `node/workspace/smoke.js`。

## 17:3x 04-new-tlab-animation.html 视觉风格对齐 02/03（用户："为啥和 02 03 不一样，还是 02 03 好"）
- **根因**：04 是另一套"米黄 paper 风"模板（bg #FBF7F0、line #E8DFCE、蓝 #5B7CFA 偏手绘），顶栏左对齐无 badges、代码面板无行号；而 02/03 是标准 Tailwind 四色（blue #3b82f6 / purple #8b5cf6 / orange #f59e0b / green #10b981）+ 居中 header + 四色 badges + 卡片阴影渐变 + 带行号代码面板。两者一眼"不是一个系列"。
- **改法（只动外壳，不动 12 步业务逻辑）**：① `:root` 配色整体换成标准四色 + 新增 `*-bg`/`*-bd` 辅助变量；② 顶栏改居中 `header` + 四色 `badge`（0xbb new / TLAB bump / 慢路径三段式 / C2 标量替换）；③ 步骤胶囊 `.cap`→`.step-pill`（JS 里 `caps`→`steps` 同步）；④ 控制栏 `button`→`.btn`（play/prev/next/reset）+ 加进度条 `prog` + 步数 `stepCount`，render() 末尾写 `prog.width` 与 `stepCount.textContent`；⑤ 代码面板 `<pre id=codePanel>` 外包 `.code-panel`（含 `.code-head` 文件名标签 `codeFile`）+ 每行加行号 `<span class="ln">` 渲染成 `.code-line`；⑥ 堆/TLAB/old/决策面板所有硬编码米黄/旧橙紫（`#FFF9EE/#F5A623/#E89A00/#9B7BFA` 等）统一归一到四色与 `*-bg`/`*-bd`。
- **验证**：jsdom 复测 8 个动画仍全部 clean；针对性 check04.js 断言 12 个 step-pill、code-line 行号 1、codeFile 随步进从 bytecode→memAllocator.cpp、prog 6 步后 54.5%、stepCount "7 / 12"、运行时零错误。
- **现状**：02/03/04 现已统一为 `.step-pill` 模板；05/06/07/08/09/10 仍是早批的 `.chip` 类名（视觉一致、仅内部命名不同）。如需"类名也彻底统一成一套"，可后续把 05-10 的 `.chip` 批量改 `.step-pill`（低风险但涉及 6 文件，按需再做）。

## 17:4x 05-10 胶囊类名统一为 .step-pill（用户："统一下"）
- **范围**：响应上轮末尾的"统一下"，把 05-10 六个动画的步骤胶囊从早批 `.chip`/`.chips` 模板整体对齐成 04 同款 `.step-pill` 圆角 pill（与 02/03/04 彻底一致）。
- **改法（批量脚本 unify-pills.js，零手改）**：6 文件各 10 处替换全 miss=0——① CSS `.chips`→`.steps`（加 justify-content:center 居中）② `.chip{...圆形34px...}`→`.step-pill{...圆角999px padding...}`（仍显示步骤序号 i+1，保留 title=s.d 悬浮描述）③ `.chip.on`→`.step-pill.active`（蓝底+阴影）④ `.chip.done`→`.step-pill.done`（绿底）⑤ HTML 容器 `class="chips" id="chips"`→`class="steps" id="pills"` ⑥ S 对象 `chips:`→`pills:` ⑦ `querySelectorAll('.chip')`→`.step-pill` ⑧ render className `chip + on/done`→`step-pill + active/done` ⑨ buildCaps `c.className='chip'`→`'step-pill'` ⑩ `S.chips.appendChild`→`S.pills.appendChild`。CSS 引用 05-10 自有变量（--blue #4C6FAF / --green #3E9E6B / --line / --card / --sub），不依赖 04 的 Tailwind 变量。
- **验证**：grep 确认 6 文件零残留 `.chip`/`.chips`/`.chip.on`/`.chip.done`；jsdom verify-pills.js 逐一加载并模拟点"下一步"14 次，6 文件全部 `clean`、胶囊数=13、active/done 状态切换正常（末步 12 done + 1 active）。
- **两个已知边界（未动，待拍板）**：
  1. **【已解决 17:4y】步骤数 13 但计数写 /12**：用户选"1"后，已将 05-10 render 里硬编码的 `(i+1) + '/12 · '` 全部改为 `(i+1) + '/' + steps.length + ' · '`（05 用 Edit、06-10 用脚本 fix-count.js，各 1 处全命中）。验证 verify-count.js：6 文件末步 `stepDesc` 均显示 `13/13`、pills=13、totalShown=13、零运行时错误。
  2. **【已解决 17:4z】整体配色统一为 04 标准四色**：用户说"继续做把"后，将 05-10 的米黄手绘风整体替换为 02/03/04 的标准 Tailwind 四色。做法（脚本 recolor.js）：① 全局 hex 翻译表 37 项（旧米黄→新四色，如 #4C6FAF→#3b82f6、#FAF6EF→#f6f7fb、#E4DCC8→#e5e7eb、#2d2a24→#111827、#B8A98A→#9ca3af、各浅色调 #f2ede2/#fff8f0/#faf4fd/#f2faf5/#efe8f7/#f7efdd 等→对应 *-bg 浅色），`:root` 变量值随之自动生效；② rgba 翻译 4 项（阴影 rgba(184,169,138,.35)→rgba(17,24,39,.05)、蓝 active 阴影、橙/紫点缀）。结果：6 文件 changedHex 28-35、旧色零残留，复跑 collect-colors + verify-count.js 均确认配色纯净且步进 13/13 全 clean。
  3. **【已解决 17:55】header 居中对齐 + 四色 badges**：用户"继续"后，将 05-10 的裸 `h1`（左对齐、无 badge）整体对齐 02/03/04 的居中 `header` + 四色 `badge`。做法（脚本 align-header.js）：① 补 04 已有的辅助变量（`--*-bg`/`--*-bd`/`--red*`/`--ink-2/3`/`--code-bg/ink`）进 6 文件 `:root`；② 插入 `header{}`/`header h1{}`/`header .badge{}` 等 CSS，关键用 **`header .badge` 作用域隔离**避免与 05 内部已有的 `.badge` 状态行冲突；③ 逐篇把 h1+sub 包进 `<header>`，标题用 `<span class="mono">` 包关键字节码名（05-08 包单关键字、09/10 综合篇包首词），并在真实锚点（`instanceKlass.cpp:1417`/`check_klass_subtype`/`constMethod.hpp:111`/`Object.java:471`/`rewriter.cpp:410`/`access_flags`/`jvm.cpp:1316` 等）基础上拟 4 个四色 badge。验证 verify-header.js：6 文件 `header=1 hBadge=4 innerBadge=1`(05 内部 badge 未破) `mono=1 step=count-ok CLEAN`。**至此 keywords 系列 02-10 共 9 个交互动画在配色、步骤条(.step-pill)、header/badges 三处全部统一为同一套 Tailwind 四色模板。**
- 脚本位置：`node/workspace/unify-pills.js`、`verify-pills.js`、`count-steps.js`、`recolor.js`、`collect-colors.js`、`fix-count.js`、`align-header.js`、`verify-header.js`、`smoke.js`（验证用，可留作回归）。

### 18:0x 04 new/TLAB 动画两处补实（Foo 长什么样 + 四连查逐条详解）
- **背景**：用户反馈 04 动画第①步只放 `new #7` 字节码 + 堆上一个「Foo 对象」标签块，**从没展示 Foo 类本身**（代码里那个 `java` 的 Foo 定义是死代码没被引用）；第②步「四连查」navi 只有一句流水账，看不出每关代表什么。
- **补刀 A（Foo 长什么样）**：在 step0 前插入「① Foo 是什么」一步（act='fooView'，code='java'）。堆视图 obj 被改画成 4 格横排（mark=unlocked(01) 蓝 / klass→Foo 紫 / int x=0 橙 / int[] arr=null 绿，呼应四色），navi 讲透 InstanceKlass（元空间户口本：字段表/方法表/常量池/父类 + layout_helper 算大小 + init_state 记 clinit）vs 实例 oop（堆上 4 段）+ new 只填默认值的"半初始化"。
- **补刀 B（四连查逐条）**：把原「② 四连查」一步拆成 4 步（③ 检查·CP tag / ④ 检查·klass / ⑤ 检查·clinit / ⑥ 检查·layout），act='check1'..'check4'，共用 CODE.check 代码块，renderCode 按 act 逐行高亮：i=2 cmpb(CP tag, :3746) / i=3 load_resolved_klass(klass, :3750) / i=4 clinit_barrier(clinit 屏障 static 兜底, :3756) / i=5 testl layout_helper 慢路径位(:3759)；runAct 4 分支各自 ptab 显示逐关累积进度，全过→tlab_allocate 快路径。源码锚点全部对到 templateTable_x86.cpp:3732 的 `_new` 真实四连查（已 grep 核实行号）。
- **结果**：STEPS 由 12 步扩展为 **16 步**（编号 ①-⑯），进度条/步数/胶囊全动态无需改其它逻辑；Foo 4 格解剖与 4 连查高亮经 jsdom 验证脚本 verify04_v3.js 全部 PASS，零运行时错误。
- 经验：**改交互动画 STEPS 数组（插入/拆分步骤）是低风险操作**——只要新步骤对象带正确的 `act` 字段且 runAct 有对应分支、CODE 有对应代码块即可；进度/胶囊/计数全靠 `STEPS.length` 动态，无需手工同步。验证务必"逐步快照"而非"连点 N 次后断言"，否则像 verify04_v2.js 那样会因步进错位误判高亮失败。
- 脚本位置：`node/workspace/verify04_v2.js`（步进错位版，已弃）、`verify04_v3.js`（逐步快照版，PASS）。

### 18:18 04 new/TLAB 动画「慢路径决策」步骤补实（双分支对比卡）
- **背景**：用户卡在 04 第⑫步「慢路径决策：剩余空间 > refill_waste_limit？剩余多→堆外；剩余少→退休换新」看不懂——反直觉点是"剩余多反而走堆外、剩余少反而换新"。
- **改法（6 处，纯增强不动业务逻辑）**：① CSS 加 `.branch-row/.branch{keep,retire}.active` 双分支卡（含迷你 TLAB 余量条：keep 绿 46% / retire 橙 8%）；② HTML `id="decide"` 面板内嵌两张对比卡（`branchKeep`/`branchRetire`，文案含"便签纸"比喻）；③ STEPS 第⑫步 navi 重写为"TLAB 剩余值得为这次分配丢掉吗？+ 便签纸比喻 + 源码 memAllocator.cpp:274 注释原话「too large to discard」"；④ CODE.slow2 行号改 274 并补 `Retain tlab and allocate object in shared space if the amount free in the tlab is too large to discard.` 注释（原 return nullptr→shared space）；⑤ runAct `decide` 步点亮 `branchKeep`（free=46%>waste=30%→保留走堆外）；⑥ runAct `refill` 步点亮 `branchRetire`（free=8%<waste=30%→退休换新）。两分支 active 互斥。
- **结果**：jsdom 验证脚本 verify04_v4.js **PASS**——init decide 隐藏、decide 步 keep active/retire 非 active/文案含「保留」、refill 步 retire active/keep 非 active/文案含「退休」、两卡文案均含「便签纸」、全程零运行时错误。
- 真源码锚点：memAllocator.cpp:274（`tlab.free() > tlab.refill_waste_limit()` → record_slow_allocation + return nullptr 回上层走 shared space；否则 retire_tlab + allocate_new_tlab），注释原话即这步灵魂。
- 脚本位置：`node/workspace/verify04_v4.js`（PASS）。

