# 2026-08-21

## 04 new/TLAB 动画答疑：outside_tlab 与「都在堆上」的混淆
- 用户疑问：动画第⑩步「③ outside_tlab：直接向 GC 堆要内存，大数组可能进 Humongous 区」让人困惑「不都在堆上吗」。
- 澄清要点：所有 Java 对象物理都在堆上；区分的是**分配路径/机制**而非「堆 vs 非堆」——
  ① TLAB 快路径（线程私有小块，bump-pointer 无锁）→ ② outside_tlab（TLAB 放不下，去 Eden 公共区，多线程共享需 CAS/锁）→ ③ Humongous（G1 老年代 region，对象 > region 一半）。
- 真源码锚点：memAllocator.cpp:249 `mem_allocate_inside_tlab_fast`、:235 `mem_allocate_outside_tlab`（内部调 `Universe::heap()->mem_allocate()`，仍是 GC 堆）、g1CollectedHeap.cpp:385 `humongous_obj_allocate`（老年代 region，阈值 region 一半 :1354）。
- 动画措辞问题：「③ 堆外」易与 off-heap/native（如 DirectByteBuffer 直接内存）混淆；它相对的是「TLAB 内」，物理仍在堆。建议改准确为「Eden 公共区 / 堆共享分配 + Humongous 区」。待用户确认是否改动画。
- 演示图已用 Visualizer 内联 SVG 画出「堆内三路径落点」对比（蓝 TLAB / 绿 Eden 公共区 / 橙 Humongous，外加灰色 off-heap 区明确区分）。


## 04 动画「堆外」歧义措辞校准（08-21）
- 主人质疑「③ outside_tlab：直接向 GC 堆要内存」的「堆外」措辞歧义（以为离开堆）。实则所有 Java 对象都在堆内，区别是落在 TLAB 私有缓冲 vs Eden 共享区 vs Humongous 老年代区——"堆外"相对的是 TLAB 内，非 off-heap。
- 统一把 11 处「堆外」歧义措辞改准：决策面板保留卡 / 第⑫步 navi / 第⑭步 navi / decideText / ptab / CODE.slow2（③ Eden 公共区、shared space 注释）/ CODE.big 注释。统一为「Eden 公共区（堆内，非 off-heap）」，并强调 outside_tlab 只是绕过本线程 TLAB、物理仍在 GC 堆；Humongous 是 Eden 放不下时进老年代区的特例。
- 验证：语法 OK；jsdom 步进全程零运行时错误；第⑭步 navi 含「Eden 公共区分配 / 非 off-heap / Humongous」均 PASS。脚本 replace-heapwording.js（批量）+ patch-d.js（补 D 处）。
- 经验：批量文本替换的 from 必须匹配文件真实字符——324 行 navi 里「保留 TLAB」无 <b> 标签、而 204 行 HTML 有 <b>保留</b>，两处来源不同需分别构造 from；先 grep 精确行再替换，避免凭记忆带错标签导致 hit=0。

## 05 static/clinit 动画升级（对齐 04 的「详细」标准）
- 用户指 05「也有同样的问题」——经 jsdom 验证 05 功能本就正常（13 步全前进、零报错），并非 04 那种致命语法错误；差距在**细节/显示**：05 源码面板是单块无行号文本，而 04 修好后是带行号+逐行多色高亮+注释着色的精致面板；且 05 第8步源码里 `<clinit>` 会被浏览器当 HTML 标签（渲染 bug）。
- 用户确认方向：① 代码面板做细（对齐 04）+ ③ 讲解讲细。
- 改造（upgrade05.js 一次性脚本）：
  - CSS：`.src .code` 单块 → `.code-panel/.code-head(.file)/.code-body/.code-line(.ln 行号 + .hl/.hlb/.hlg 多色 + .cm 注释)`。
  - HTML：`<div class="src">` → `<div class="code-panel"><pre id="codePanel">`。
  - JS：`hlLine` 换成 `renderCode(idx, hlMap, file)`，逐行 `split('\n')` + 转义 `& < >` + `//` 注释着色；彻底修掉 `<clinit>` 标签 bug。
  - 控制栏加 `.prog` 进度条（蓝→紫渐变）+ `#stepCount` 步数；`render()` 同步更新，desc 改用 innerHTML 渲染富文本。
  - 13 步 `d:` 讲解逐条细化（初始化锁=synchronized(mirror)、递归重入快路径、strict static 位数组校验(JDK28)、双线程竞争 T1跑/T2阻塞、错误态永久冻结），含 `<b>/<br>/<code>`，`<clinit>` 一律写成 `&lt;clinit&gt;`。
- 验证：jsdom 逐点步进零报错；第6/7步源码面板 `&lt;clinit&gt;` 正确转义、无 raw `<clinit` 标签；行号/高亮/注释着色正常；进度条 0%→100%、步数 1/13→13/13 同步；smoke 仍 clean。
- 注意：06-10 当时只做了 pills/配色/步数/header 统一，**源码面板仍是无行号单块**，与 05 旧版同款差距；若主人觉得 05 这版好，可同样给 06-10 升级（但需先确认）。

## 04 第①步 Foo 内存布局图（手绘 SVG）
- 用户要求「Foo 长什么样子得画出来吧」——原第①步只是堆里 4 格示意条（mark/klass/x/arr），缺字节级细节。
- 改动：在 heap 内新增 `#fooDia`（白底卡片，仅 fooView 步 `display:block`），内嵌手绘风 SVG（viewBox 0 0 680 300，配色限蓝/紫/橙/绿四色）：
  - 主条按字节比例拆 5 段：mark word 8B(蓝) / 压缩 klass 4B(紫) / int x 4B(橙) / arr 引用 4B(绿) / 对齐填充 4B(灰虚线)=24B（3 堆字，8 字节对齐）。
  - 底部字节偏移标尺 0/+8/+12/+16/+20/+24。
  - klass 段画紫色曲线箭头指向右上「InstanceKlass（元空间 Metaspace）」卡片（含 layout_helper / init_state / 字段表方法表常量池父类）。
  - 左下说明框「new 此刻干了什么」：切 24B oop → 填 mark=prototype(unlocked)/klass→Foo → 字段清零 x=0/arr=null → 半初始化对象。
- runAct 顶部统一 `$('fooDia').style.display='none'; $('obj').style.display=''; $('obj').style.opacity='1';`（顺带修了 c2 步后 obj opacity 不复位的小问题）；fooView 步内 `obj` 隐藏 + `fooDia` 显示。navi 文案改为引用字节级布局。
- 验证：jsdom 确认 step1 fooDia=block/obj=none+SVG(7 rects+InstanceKlass+mark word++24)；step2 fooDia=none/obj=block；点胶囊跳回 step1 再次显示；零 JS 报错；smoke 04 仍 clean。

## 05 答疑：静态成员 + Foo 源码缺口 + y 标签矛盾
- 用户问「什么是静态成员，foo 里面有吗」——根因：05 动画只通过字节码/flag 引用 `Foo.x`/`Foo.y`，**从没把 `class Foo` 源码画出来**，静态成员一直抽象。
- 概念答复：静态成员属「类」（InstanceKlass 的 Java mirror，非实例 oop），全类一份、用 `Foo.x` 访问；与 04 动画里 Foo 的实例字段 `int x`（在堆 oop、每对象一份）正对照。Foo 确有静态成员：`static int x`/`static int y`，正是 `getstatic Foo.x` 触发 `<clinit>` 的东西。
- 修复（05）：① 第①步 navi 注入 `<pre>` 块展示 `class Foo { static int x; static int y; static { x=42; y=7; } }`（单引号 JS 串内用 `\n` 转义换行，避免裸换行语法错）；② 发现并修掉内部矛盾——flag 面板 `Foo.y（仅声明，永不赋值 → 异常）` 与第9步「y 也赋过值→校验通过」+`flag('fY',true)` 打架；把 fY 标签改为 `Foo.y（putstatic 赋值）` 对齐 happy-ending（校验通过）。保留第7步 strict-static 规则讲解"永不赋值=非法"（那是规则，非矛盾）。
- 验证：jsdom step1 navi 含 `<pre>`/`class Foo`/`static int x`/`static int y`，fY 标签=putstatic 赋值，旧 flag 串已移除；零 JS 报错；smoke 05 仍 clean。

## 05 答疑：strict static 深度讲解补入
- 用户反馈"strict static 有点不太理解"——根因：动画 step 7/9 虽提了 strict static 位数组，但没讲**它解决什么问题**、**位在哪里设的**、**getstatic 读前写会抛异常**等关键语义。
- 源码核查（OpenJDK 28 mainline，D:/project/jdk/src/hotspot）：
  - `fieldInfo.hpp:324-358`：`FieldStatus` 位数组，4 个 bit（access_watched/modification_watched/strict_static_unset/strict_static_unread/initialized_final_update）。
  - `classFileParser.cpp:6434-6453`：类加载时发现 `is_strict() && is_static()` 字段（非 ConstantValue）→ 置 `unset=1, unread=1`。
  - `interpreterRuntime.cpp:749-761`：`<clinit>` 中 putstatic/getstatic 命中 unset=1 字段 → `notify_strict_static_access`（:1669）：putstatic 清 unset 位（:1698）；getstatic 抛 ISE"is unset before first read in"（:1703）。
  - `instanceKlass.cpp:1587-1614`：`<clinit>` 跑完 → 遍历 fields_status → 任何 unset=1 → 抛 ISE"is unset after initialization of" → initialization_error。
  - `AccessFlag.java:286-297`：`ACC_STRICT_INIT = 0x0800`，JDK 28 preview（`PreviewFeature.Feature.STRICT_FIELDS`），需 `--enable-preview`。
- 讲解补入动画（enhance05-strict.js）：
  - SRCS[4] 源码面板从伪码换成真实校验循环（for index/length + is_strict_static_unset + throw_strict_static_exception）。
  - step 7 增强：讲清"什么是 strict static"（解决声明不赋值 bug）、"位在哪里设的"（classFileParser:6434）、"putstatic 清位"（:1698）、"读前写=非法"（:1703）。
  - step 9 增强：三种结局（全清→通过 / 某位=1→ISE→error / ConstantValue 豁免:6443）+源码注释引用"bits must all be clear"。
- 验证：jsdom step7 含 ACC_STRICT_INIT/classFileParser/读前写；step9 含 ConstantValue/完整 for 循环；`<clinit>` 转义正常；零报错；smoke clean。
- 用 Visualizer 画了 strict static 生命周期图（三阶段：类加载置位→clinit 中 putstatic 清位/getstatic 抛→clinit 后遍历校验）。

## 07 异常动画 13 步讲解全面通俗化（08-21）
- 用户反馈 07「每一步都看不懂」——根因有二：① 内容跳度大，全是术语轰炸（异常表 4×u2/bci/athrow/catch-all/解栈/TLS/is_subtype_of 都没解释直接拿来用）；② **渲染致命伤**：07 的 `render()` 用 `S.desc.textContent` 设讲解（不是 innerHTML），富文本标签显示为字面字符且整段 `font-weight:bold` 粗体，一坨粗体术语不可读。
- 改造（upgrade07.js 替换 + fix07.js 修引号）：
  - CSS：stepDesc 从整段粗体改为 normal+line-height:1.75，新增 `.stepDesc b{color:var(--blue)}`、`.stepDesc code{背景高亮}`。
  - render：`S.desc.textContent` → `S.desc.innerHTML`（富文本真正渲染）。
  - 13 步 d 全部重写为「一句话结论 + • 分点拆解」结构，每个核心概念首次出现必解释：救援地图（异常表）、u2、finally 复制①②、athrow 0xbf、逐帧查表/解栈/TLS(vm_result)、[start,end) 半开区间、is_subtype_of 血缘判定（复用 06 篇）、栈不拆、catch-all 兜底重抛、红色堆栈来源。
  - **教训**：07 的 d 字段是单引号字符串 `'...'`，不能跨行；替换成多行文本导致 SyntaxError「Invalid or unexpected token」（整段 script 失效，jsdom 报 Uncaught）。修法：正则 `\{ d:'([\s\S]*?)',\n(\s*)fn:` → 反引号模板字符串 `{ d:\`...\``（13 处全中，需确认 body 不含 `'`/反引号/`${`）。**经验：向 HTML 内联 JS 注入长文本前先确认原字符串是单引号还是模板串。**
- 验证：jsdom 13 步全前进、零报错；innerHTML 含 `<b>/<code>` 且 textContent 无字面标签（渲染生效）；pills=13；抽查 step0/4/10 讲解完整清晰；smoke 07 clean。
- 提示：08-10 仍是旧式 textContent+纯文本讲解风格，若主人觉得 07 这版好可同样升级（需先确认）。

## 07 答疑：异常表 [start,end) handler 拆解（08-21 续）
- 用户问「[start,end) handler 得解释下」——根因：07 第3步(异常表结构)只讲了"4 个字段叫什么"（u2/start_pc/end_pc/handler_pc/catch_type_index），但**没解释 bci 是什么、9 是什么、为什么 [0,3) 不写 [0,2]**，读者无法把表头和右栏字节码对上。
- 讲解核心（补入第3步 d 字段，patch07-step2.js 精确替换）：
  - **bci = 字节码门牌号**：右栏每行最左数字（0/3/6/9/13/16）就是门牌，异常表里一切数字都是门牌号，不是行号/类名。
  - **① start_pc** 救援范围起始门牌；**② end_pc** 范围"最后一条的下一条"门牌（不包含）→ 半开区间 [start,end)；**③ handler_pc** 跳转目标门牌；**④ catch_type_index** 只救该类型，0=catch-all。
  - **为什么半开**：字节码指令长短不一（invokestatic 3B / astore_1 1B），数"最后一条的字节偏移"难算，编译器算"下一条"最省事 → 约定 end 不包含。
  - 翻译人话：`#0: [0,3) → 9 → IOException` = 门牌 0/1/2 三条指令内抛 IOException(或其子类) → 搬到门牌 9（astore_1 存异常→println）；门牌 3 是 try 外 finally 复制①，不属于保护范围——正是写 3 不写 2 的原因。
- 验证：jsdom 第3步含门牌号/半开区间/长短不一/end 不包含/住在门牌等全部关键词，textContent 无字面 `<b>`，零报错；smoke 07 clean。
- 经验：jsdom 验证前先数清步骤数组索引（07 顺序=1救援地图/2finally/3异常表结构，要验证第3步需点 2 次 next），避免断言错位误判 false。

## 07 加示例代码总地图 + 每步引用（08-21 续）
- 用户反馈「没有写示例代码不好理解，第四步之后就不太理解了」——根因：07 从头到尾没有完整 Java 示例源码，只有零散的 risky()/cleanup()/try-catch-finally 字样；第 4 步（risky 解栈）起全靠"读者已知 main/risky/catch/finally 长什么样"的隐含前提，越往后越抽象。
- 改造（upgrade07b.js 精确替换 10 处 + 手工 CSS/HTML）：
  - **示例代码总地图面板**（`.sample`，stepDesc 与 grid 之间，全宽）：15 行 Java 源码带行号（main 帧 1-9 行 / risky 11-13 行 / cleanup 15 行），每行右侧标 bci 门牌（`← bci 0 ── try 块 [0,3)`、`← handler 9`、`← bci 3/13`、`← athrow 0xbf`），语法着色 kw/ty/cm/bci 四类。作为全片唯一剧情的对照物。
  - **步骤引用**：index 0 + 4-12 共 10 步，讲解开头插入「看示例代码第 X 行」引导，把抽象概念钉在具体代码行上（如 index 4 → 第 11~13 行 risky 无 try→异常表空；index 6 → 第 3 行 bci 0~2；index 7 → 第 4 行 catch(IOException)；index 8 → 第 4~5 行 handler 9；index 9 → 第 6~8 行 finally 复制 ×2；index 10 → 换 SQLException 时第 4 行接不住）。
- 验证：jsdom sample 面板 15 行/含 main/risky/cleanup/bci 标注/FileNotFoundException；index 4-12 每步含「示例代码第 X 行」；零 JS 报错；smoke 07 仍 clean。
- 经验：jsdom 验证"某步含引用"要按索引推点击次数，且末步重复点击不前进（i=11 与 i=12 同是 index 12）。
## 08 final 动画：补 final 类面孔 + 讲解全面重写（08-21 续）
- 用户反馈 08「还可以修饰类」+「每一步的解释我都看不懂」——两个问题：① final 修饰类只在原第10步一行注释带过，无任何讲解；② 13 步 d 全是旧式 textContent 单句术语轰炸（07 重写前的同款毛病），且 render 用 textContent 导致 `<b>/<code>` 不可用。
- 重写（Write 全量覆盖 08-final-animation.html）：
  - **三副面孔 → 四副面孔**：导航加 face4「final 类 · 禁继承 · ICCE 防线」（紫色），CSS 补 `.face.f4 .num/.f4.on`，faces() 支持 4。
  - **15 步（0-14）**：0 初始四面孔总览 / 1-3 面孔①（常量表达式、ConstantValue、重编译坑）/ 4-5 面孔②（wait0 铁证、vtable 槽）/ 6-8 面孔③（freeze、do_exits、x86 空编码）/ 9-10 面孔④（final 类引入、VM 防线）/ 11 strict 豁免 / 12 四面孔对比 / 13 final vs volatile / 14 系列闭环。
  - **面孔④真实源码证据**（本地 OpenJDK 28 mainline 挖出）：classFileParser.cpp:6326-6329 解析到 `_super_klass->is_final()` → `classfile_icce_error` → **IncompatibleClassChangeError**（classFileError.cpp:90 确认异常类型）；check_super_class_access :4294-4297 同检查；:3798-3801 final 与 sealed(PermittedSubclasses) 互斥；ciMethod.hpp:353 `is_final_method()=is_final()||holder()->is_final()`（final 类方法全隐式 final）；ciInstanceKlass.hpp:324 `exact_klass()`（final 类 → C2 精确类型，instanceof 直接比较，绕开 06 篇 super_check_offset 查链）。
  - **讲解重写**：13 个 d 全改反引号模板字符串（防单引号跨行），每步「一句话结论 + `<b>①` 分点 + 术语首现解释」；render 改 `S.desc.innerHTML`；CSS 加 `.stepDesc{font-weight:normal;line-height:1.85}` + `.stepDesc b`(蓝) + `.stepDesc code`(浅紫带底)。
  - header/sub/badges 同步更新（加 red badge「final class · ICCE」）；第10步 src 面板贴 classFileParser.cpp:6326 截取 + ciMethod.hpp:353 + ciInstanceKlass.hpp:324 三块真源码。
- 验证（verify08.js）：pills=15；15 步 desc 逐条断言 OK（含「final 类 = 禁继承」「IncompatibleClassChangeError」）；face4 存在、step9 高亮 on；step9 src 含 ACC_FINAL；step10 src 含 cannot inherit from final class / ciMethod.hpp:353 / ciInstanceKlass.hpp:324；连点 16 次 smoke 停在 15/15；零 jsdom 错误、零断言失败。
- 经验：final class 的运行时检查抛的是 **ICCE 不是 VerifyError**（JVMS 层面笼统说 5.4.4，HotSpot 实际走 classfile_icce_error → IncompatibleClassChangeError），写动画前务必 grep classFileError.cpp 确认异常类型，别凭 JVMS 记忆猜。

## 09/10 动画：讲解全面重写（08-21 续）
- 用户反馈 09/10「是不是过于简单的解释了」——同 07/08 的旧毛病：13+13 步 d 全是单句术语轰炸（如 09 第4步"计数器溢出→OSR：解释器帧原位替换"），无分点无术语解释；render 用 textContent 导致 <b>/<code> 失效。
- 重写（Write 全量覆盖两个文件，fn() 的 stage/src/scene 源码证据面板全部保留不动——行号已在前几轮核实）：
  - 26 个 d 字段全部改反引号模板字符串，每步「一句话结论 + <b>① 分点拆解 + <code>术语解释」，术语首现必解释（OSR/回边/TosState/safepoint/掩码/受限关键字/全限定名...）
  - render: S.desc.textContent → S.desc.innerHTML
  - CSS: .stepDesc 加 font-weight:normal + line-height:1.85；新增 .stepDesc b(蓝) / .stepDesc code(浅紫带底)
- 验证（verify0910.js）：13+13 步逐条断言关键词 + <b> 结构存在 + textContent 无字面 "<b>" + 连点冒烟停在 13/13 + 零 jsdom 错误。首跑 2 处失败均为断言文案与实际 d 不匹配（"闭环"vs"串起来"、"位复用"vs"同一个位"），修断言后全过。
- 经验：断言关键词必须从实际 d 文案里抄，别凭印象写；讲解断言两件套 = 关键词在 textContent 里 + <b> 在 innerHTML 结构里 + textContent 不含字面 "<b>"。

## 方法调用全链路研读 · 第一站 linkResolver（08-21）
- 用户确认开始啃「方法调用全链路」主线，第一站 interpreter/linkResolver.cpp 符号解析，全部行号在本地 OpenJDK 28 核实。
- 关键结构：
  - resolve_invoke (:1715) 纯分发器，6 字节码各走各路（static/special/virtual/interface/handle/indy）。
  - LinkInfo 构造 (:259/:275) = 常量池懒解析入口：pool->klass_ref_at (:918) → klass_at_impl (:631) → SystemDictionary::resolve_or_fail 首次触发类加载。
  - linktime 七步 resolve_method (:753)：①virtual 调接口→ICCE(:760) ②tag≠Methodref→ICCE(:769) ③类链查找 lookup_method_in_klasses(:356，skip overpass+default methods) ④接口链(:442)+JSR292 polymorphic(:787) ⑤NoSuchMethodError(:803) ⑥访问检查(:813) ⑦loader约束(:821)。
  - resolve_interface_method (:862) 同构，但先验 is_interface(:867)+InterfaceMethodref tag(:875)。
  - runtime 分界：runtime_resolve_virtual_method (:1412) — null→NPE(:1429)；接口方法(default/miranda)→vtable_index_of_interface_method(:1440)；类方法→vtable_index(:1450)；nonvirtual_vtable_index(final/private)→静态绑定(:1457)；否则 recv_klass->method_at_vtable(:1464)。runtime_resolve_interface_method (:1525)：接收者未实现接口→ICCE(:1540)、lookup_instance_method_in_klasses(:1561)、非public→IAE(:1580)、三分支 vtable/itable/nonvirtual(:1599-1617)。
  - CallInfo::set_virtual (:93) 关键 :99：vtable_index>=0 && !can_be_statically_bound → vtable_call，否则 direct_call。set_common(:138) 写回 ConstantPoolCache → 热路径免解析。
- 核心认知：解析只在冷路径发生一次并写缓存；热路径=CPCache 查 index + method_at_vtable 一次查表。linktime 看编译期静态类型(resolved)，runtime 看接收者实际类型(selected)——多态语义的落点。
- 下一站：oops/klassVtable.cpp（vtable/itable 构建与查找）+ templateTable_x86 invoke 模板 + C2 doCall 去虚化/内联。

## 方法调用全链路 · 第一站三件套产出（08-21 续）
- 用户要求「输出文档呀」「和之前一样呀，输出文档和交互动画」——把 linkResolver 第一站研读成果按 keywords 系列老规矩做成三件套，新目录 D:/project/jdk/.workbuddy/docs/method-call/：
  1. **01-linkresolver-symbol-resolution.md**：快速概览 + TOC + 编号小节（一~八）+ 冷热路径全景图引用 + 行号速查表；七步错误分类表（ICCE/NSME/IAE/loader 约束）；衔接表（08 direct_call、06 itable、09 TosState）。
  2. **01-linkresolver-overview.svg**：冷路径（蓝 4 节点：分发→linktime→runtime→缓存）vs 热路径（绿 3 节点：缓存命中→method_at_vtable→直接调用）全景手绘 SVG，四色配色。
  3. **01-linkresolver-animation.html**：完全对齐 08 骨架（faces 四关卡导航：入口分发/linktime/runtime/缓存热路径；stage 舞台；src 源码证据面板；16 步反引号 d 字段；innerHTML 渲染；.stepDesc b/code 样式；进度胶囊）。16 步从 javac 生成 invokevirtual → 六路分发 → LinkInfo 懒加载 → 七步①-⑦ → runtime virtual/interface 分派 → set_virtual/direct_call → 冷热路径总结。
- 验证（verify01-linkresolver.js）：16 pills；16 步逐步点击断言讲解关键词 + <b> 结构 + 无字面标签泄漏；src/stage 汇总断言 13 个源码关键词（linkResolver.cpp:1715/klass_ref_at/OverpassLookupMode/can_be_statically_bound/method_at_vtable/is_subtype_of/NoSuchMethodError 等）；连点 20 次冒烟停在 16/16；零 jsdom 错误。首跑 15 处失败全部是断言文案与实际 d 不匹配（如「switch (byte)」在 stage 不在讲解），修正断言后全过。
- 经验：源码关键词断言要分三层——讲解层（d 文案）、舞台层（stage）、源码层（src），别只查 desc.textContent；关键源码片段在写动画前必须重新 Read 原文，禁止凭记忆贴源码。

## 方法调用全链路 · 第二站 klassVtable 三件套产出（08-21 续）
- 用户「继续」——按计划进第二站 oops/klassVtable.cpp（vtable/itable 分派表），产出三件套到 D:/project/jdk/.workbuddy/docs/method-call/：
  1. **02-klassVtable-dispatch-tables.md**：快速概览 + TOC + 十节编号结构（为什么两张表/内存布局/三步建表/不进表四类/覆写合并/miranda+default/O(1) 查找/itable/与 linkResolver 衔接/行号速查表）；vtable vs itable 对比表。
  2. **02-klassVtable-overview.svg**：内存布局条（Klass 头|vtable|itable 三连）+ 上下两半四象限（左蓝 vtable 构建三步 / 右紫 vtable 查找 O(1) / 左橙 itable 构建 / 右绿 itable 线性扫描），四色手绘。
  3. **02-klassVtable-animation.html**：对齐 01 骨架（faces 四关卡：vtable 构建/运行期查找/itable 构建+查找/布局与闭环；16 步；反引号 d；innerHTML；.stepDesc b/code；进度胶囊）。
- 关键证据（本地 OpenJDK 28 核实）：vtableEntry=单 Method*（klassVtable.hpp:194）；start_of_vtable（klass.inline.hpp:112）；start_of_itable=vtable 尾（instanceKlass.inline.hpp:38）；compute_vtable_size_and_num_mirandas:66；initialize_from_super:132；initialize_vtable:161（复制前缀→update_inherited_vtable 覆写/追加→default/miranda 收尾）；needs_new_vtable_entry:633（final/private/static/<init> 不进表，package-private 自己当根）；update_inherited_vtable:382（name+sig 匹配→put_method_at:504 替换+set_vtable_index:512）；find_transitive_override:309；fill_in_mirandas:959；method_at_vtable=start_of_vtable()[i].method()（klass.cpp:1118）O(1)；compute_itable_size:1506（接口数+1 offset + 方法总数 method）；assign_itable_indices_for_interface:1244（static/private/<init>/<clinit> 不编号 :1232）；initialize_itable_for_interface:1316（lookup_instance_method_in_klasses:1334 skip private 找实现→空槽 AME :1337/非 public 塞 IAE 替身 :1341/找到 initialize :1355）；method_at_itable_or_null:3876 线性扫描 offset 表（接口匹配→first_method_entry+i 直取）；method_at_itable:3849（空槽→AME :3858 / 未实现→ICCE :3872）。
- 验证（verify02-klassVtable.js）：16 pills；16 步三层断言（desc/stage/src）全过；连点冒烟停在 15/15；零 jsdom 错误。首跑 13 处失败是验证脚本点击累积老坑（每迭代点 i 次 next 未先 reset，cur 被累加），加 resetBtn.click() 后剩 1 处断言文案（"占位"vs 实际"抽象槽"），修断言后全过。
- 经验：验证脚本循环里必须"每迭代先 reset 再点 i 次"，点击位置不累积；断言关键词从实际 d 文案抄。
- 下一站预告：cpu/x86/templateTable_x86.cpp 的 invoke 模板（解释器执行链路）→ opto/doCall.cpp + bytecodeInfo.cpp（C2 去虚化/内联）。

## 方法调用全链路 · 第三站 templateTable 三件套产出（08-21 续）
- 用户「继续」——按计划进第三站 cpu/x86/templateTable_x86.cpp（解释器 invoke 模板），产出三件套到 D:/project/jdk/.workbuddy/docs/method-call/：
  1. **03-templateTable-invoke-templates.md**：快速概览 + TOC + 十二节编号结构（模板概念/四入口/RME 结构/热路径寻址/冷热分岔/clinit_barrier/prepare_invoke/压返回地址/双分支/一条 movptr/interface 三分支/两遍扫描/汇编/最后一跳/三站闭环/行号速查表）；flags 六位表；三站闭环 ASCII 图。
  2. **03-templateTable-overview.svg**：三段式管线图（蓝热路径寻址 5 步 → 紫 prepare_invoke 2 件事 → 橙/绿三分派 + 底部最后一跳），四色手绘。
  3. **03-templateTable-animation.html**：对齐 01/02 骨架（faces 四关卡：模板入口/热路径寻址/vtable 分派/interface 三分支；17 步 0-16；反引号 d；innerHTML；.stepDesc b/code；进度胶囊）。
- 关键证据（本地 OpenJDK 28 核实）：
  - **重要新发现：JDK 28 用 ResolvedMethodEntry（RME）取代老 ConstantPoolCacheEntry**——resolvedMethodEntry.hpp:68 结构（_method + union{_interface_klass/_resolved_references_index/_table_index} + _cpool_index/_number_of_parameters/_tos_state/_flags/_bytecode1/2）；flags 六位（is_vfinal=0/is_final=1/is_forced_virtual=2/has_appendix=3/has_local_signature=4/has_resolved_ref=5 :119-126）；注释 :60 明确 invokevirtual+invokespecial 可共享条目（双字节码槽的由来）。
  - 四入口：invokevirtual :3479 / invokespecial :3496 / invokestatic :3515 / invokeinterface :3539；统一三段式。
  - load_resolved_method_entry_special_or_static :2489 / _interface :2533 / _virtual :2569——全走 resolve_cache_and_index_for_method :2301。
  - load_method_entry（interp_masm_x86.cpp:1855-1863）热路径寻址 5 条汇编：帧取 CPCache → get_cache_index_at_bcp 读 u2 → method_entries_offset 定位数组 → imull index×sizeof(RME) → lea 基址+偏移。
  - 冷热分岔 :2324 cmpl temp,code → equal=L_done（热）/ 否则 InterpreterRuntime::resolve_from_cache :2343（冷，回 01 站）；invokestatic 特例 :2327-2336 clinit_barrier（类初始化检查）。
  - prepare_invoke :3394-3433：非 static/indy 取 receiver（num_parameters 回退一格 :3410-3414）；按 tos_type 从 invoke_return_entry_table_for(code) 查返回地址压栈 :3418-3427。
  - invokevirtual_helper :3435-3477：andl is_vfinal → 直接跳（null_check :3457 + jump）；否则 load_klass → profile_virtual_call → lookup_virtual_method → jump。
  - **lookup_virtual_method（macroAssembler_x86.cpp:4035-4044）= 1 条 movptr**（recv_klass + index×8 + vtable_start_offset + method_offset）——多态全部汇编成本。
  - invokeinterface 三分支 :3552 forced_virtual→invokevirtual_helper（Object 方法特例）/ :3566 vfinal→load_klass+check_klass_subtype :3576（失败→ICCE）+直接跳 :3589 / 常规 :3593 load_klass → 第一遍 lookup_interface_method(return_method=false) :3603 验身份 → restore_bcp :3611 → load_method_holder+itable_index 还原 :3615-3618 → 第二遍 :3622 取方法 → null→no_such_method→AME :3634-3649。
  - lookup_interface_method（macroAssembler_x86.cpp:3841-3912）：vtable_length → lea itable 头 → peel 剥皮循环比对 interface → 哨兵 null→no_such_interface（ICCE）→ 命中读 offset → klass+offset+index×8 二次寻址。
  - jump_from_interpreted（interp_masm_x86.cpp:715-732）：prepare_to_jump + jmp Method::from_interpreted_offset；JVMTI 时走 interpreter_entry。
- 验证（verify03-templateTable.js）：17 pills；17 步三层断言（desc/stage/src）+ <b> 结构 + 无字面泄漏 + 连点冒烟停在 16/16 + 零 jsdom 错误。首跑 1 处 JS 语法错误（step16 src( 模板字符串缺闭合 )，`}` 后少 `)`）修复后；1 处断言文案（"两遍扫描"vs 实际"第一遍"）修断言后全过。
- 经验：src( 模板字符串结尾易漏 `)`——写多行源码模板后必须自查"函数调用括号成对"；验证脚本的 stage 断言关键词从实际舞台文案抄。
- 下一站预告：opto/doCall.cpp + opto/bytecodeInfo.cpp（C2 去虚化/内联）——解释器链路的"快"止步于此，JIT 把多态压成直接调用/内联。

## 方法调用全链路 · 第四站 doCall/bytecodeInfo 三件套产出 + 验证全绿（08-21 续）
- 用户「继续」——进第四站 opto/doCall.cpp + opto/bytecodeInfo.cpp（C2 去虚化/内联），产出三件套到 D:/project/jdk/.workbuddy/docs/method-call/：
  1. **04-doCall-bytecodeInfo-inlining.md**：快速概览 + TOC + 十三节编号结构 + 行号速查表（doCall.cpp:94/550/1169/1194/1203/1238/1250/1257、method.cpp:854/876、ciMethod.cpp:137/841、callGenerator.cpp:116/183/254/1070、bytecodeInfo.cpp:116/364/531/564、c2_globals.hpp:475/700/771/790、globals.hpp:1378/1385/1389）。
  2. **04-doCall-bytecodeInfo-overview.svg**：900x660 四段图（invokevirtual #12 → ① do_call 三问(橙) → ② optimize_virtual_call 四连问(蓝) → ③ call_generator 分路(紫) → ④ generate 三种生成器(绿)），底部内联账说明。
  3. **04-doCall-bytecodeInfo-animation.html**：19 步四关卡动画（比 03 站多 2 步：关卡④拆成三种生成器/守卫菱形 IR/内联账/预算收尾+闭环 4 步）。
- 关键证据（本地 OpenJDK 28 核实）：四段决策链 Parse::do_call(:550) 三问 → optimize_virtual_call(:1169) 四连问 → call_generator(:94) 决策总闸 → cg->generate；call_does_dispatch out-参数乐观假设 false，四连问全落空才翻 true；四连问=① can_be_statically_bound(method.cpp:876) ② receiver 数组/Object 天然单态(排除 finalize :1216) ③ CHA find_monomorphic_target(:1238) ④ actual_receiver_is_exact(:1257)；final 闭环 Method::is_final_method(method.cpp:859)→08 篇 final class 隐式 final 依据；CHA 硬绑必须 dependencies()->assert_unique_concrete_method(:1250)；决策总闸[0]intrinsic→[A]ok_to_inline→[B]profile 守卫(TypeProfileMajorReceiverPercent=90/UseBimorphicInlining)→[C]CHA 接口单实现者(unique_implementor 双依赖)→[D]兜底 for_virtual_call/for_direct_call；三种生成器 ParseGenerator(callGenerator.cpp:128)/DirectCallGenerator(set_optimized_virtual)/VirtualCallGenerator(CallDynamicJavaNode)；守卫菱形 PredictedCallGenerator(:1019) type_check_receiver → miss(uncommon_trap Action_maybe_recompile :302-303)+hit(内联) → Region(3)+Phi 汇合；内联账 ok_to_inline(:564)→check_can_parse(:531 native/abstract/unbalanced monitors)→should_inline(:116 freq=call_site_count/invoke_count, InlineFrequencyRatio=0.25)→try_to_inline(:364 accessor 无条件/MaxRecursiveInlineLevel=1/DesiredMethodLimit)。
- 验证（verify04-doCall.js）：19 pills；19 步 desc/stage/src 三层断言 + scene 断言 + 每步计数器格式（第 i / 18 步）+ 连点冒烟 + 四关卡导航（step4→关卡2/step9→关卡3/step15→关卡4）全过。
- 修复记录：① **动画真实 UX bug**：go() 末尾 `S.scene.textContent='第 X / 18 步'` 覆盖了 fn() 里 scene('场景 X…') 的描述文案（用户看不到场景标题）——改为 `innerHTML += 计数器 span`，fn() 每次先重置场景再追加，不重复；② 断言词 4 处修正：step6 stage `abstract`→`抽象类`（舞台写的是中文"抽象类 A"）；step8 的 klass_is_exact 移到 desc（:1234 真实存在），stage 改 actual_receiver_is_exact（:1257 舞台展示的正是它，两个都是真源码标识符）；step13 的 Action_maybe_recompile 从 desc 移到 stage+src（:302-303 真实代码，desc 只写了中文描述），src `doCall.cpp:293`→`doCall.cpp:297`（菱形区间真实起止 :297-327）。
- 经验：① 动画 step.fn() 里 scene() 设置的文案若会被 go() 末尾统一覆盖，属于动画本身缺陷，应修动画而非只迁就断言；② 断言关键词命名冲突时先到真实源码 grep 确认两个标识符各自的出处，再决定放哪个断言层；③ 描述性文案写中文、源码标识符写英文，断言层要分开（desc 中英均可、stage/src 必须英文标识符）。
- 四站闭环叙事收尾：「一条 invokevirtual 的一生」——01 linkResolver 符号解析 → 02 klassVtable 分派表 → 03 templateTable 解释器快路径 → 04 doCall/bytecodeInfo C2 去虚化+内联；呼应 08 篇 final 与 03 站 profile_virtual_call。

## 方法调用全链路 · 第五站 Code_Gen/nmethod 三件套产出 + 验证全绿（08-21 续）
- 用户「继续」——进第五站 Compile::Optimize 之后的代码生成与 nmethod 落地，产出三件套到 D:/project/jdk/.workbuddy/docs/method-call/：
  1. **05-codegen-nmethod.md**：快速概览 + TOC + 十五节编号结构 + 行号速查表（c2compiler.cpp:125、compile.cpp:659/887/946/153/159/3795-3799/3805/3827/3834/3847/3851-3854/3870/3872/3883/3891、matcher.cpp:216、chaitin.cpp:356/380/383/393、output.cpp:253/270-275/294-297/311-322/3302/3331-3346、ciEnv.cpp:977/998/1008/1011-1017/1020-1034/1043/1046/1070、nmethod.cpp:1090/1109/1111-1138/1149-1160/1186/1211-1228/2215/2225/2249/2253/2258/2270/2277/2293/2299/2320、nmethod.hpp:549-556/690-694、dependencyContext.cpp:92、method.cpp:1441-1464、instanceKlass.cpp）。含「一条 invokevirtual 的一生」五站闭环 ASCII 图。
  2. **05-codegen-nmethod-overview.svg**：900x670 手绘五段图（编译驱动蓝 → ① Matcher+PhaseCFG 蓝 → ② PhaseChaitin 紫 → ③ PhaseOutput 橙 → ④ nmethod 落地绿 → 底部赌输撤回 make_not_entrant 深橙），右侧竖直虚线回退箭头「依赖失效 → 重新编译（闭环）」。
  3. **05-codegen-nmethod-animation.html**：20 步四关卡动画（比 04 站 19 步多 1 步）——关卡① 指令选择与调度（Matcher::match :216 / PhaseCFG :3827 / do_global_code_motion :3834 / Matcher 不回收 :3795）→ 关卡② 寄存器分配（PhaseLive :380 / PhaseIFG :383 / de_ssa :393 / 着色+溢出 / 收尾流水线）→ 关卡③ 机器码生成与安装（Prolog :270-275 / UEP :294-297 / Epilog :311-322 / install 定入口 :3331-3346 / register_method 六道门禁 :977-1046）→ 关卡④ nmethod 落地与失效（new_nmethod 空间账本 :1090-1138 / 依赖登记 :1149-1160 + dependencyContext.cpp:92 头插 CAS / set_code 挂牌 method.cpp:1441-1464 / 状态机 nmethod.hpp:690-694 + make_not_entrant :2215-2290 / unlink→purge 回收 :2293/:2320 + 五站闭环）。
- 关键证据（本地 OpenJDK 28 核实）：编译驱动 while(!env->failing()) 重试循环 c2compiler.cpp:125，失败降级关 SubsumeLoads(:153)/DoEscapeAnalysis(:159) 越退越保守；Matcher 不能回收因为生成 spill 代码还要用（compile.cpp:3795-3799 注释）；PhaseChaitin 分配后 use-def 链失效但 Node→LRG→reg 映射仍准（:3851-3854）；Output() 用 MachPrologNode 替换 StartNode（:270-275）、非静态方法插 MachUEPNode（:294-297）、每 return 前插 epilog（:311）；install_code 定入口偏移——普通编译 Verified_Entry=_first_block_size、OSR 编译对调（:3331-3346）；register_method 六道门禁 ciEnv.cpp:977——method counters(:998)/CodeCache::gc_on_allocation(:1008)/双锁 MethodCompileQueue_lock+Compile_lock+NoSafepointVerifier(:1011-1017 防 add_to_hierarchy)/Jvmti-DTrace(:1020-1034)/依赖编码+验证(:1043/:1046)/new_nmethod(:1070)；new_nmethod 三块空间 nmethod.cpp:1090——主体 CodeCache placement new(:1134)、immutable_data C heap os::malloc(:1122 含 PC 描述/依赖/异常/调试表)、mutable_data 重定位，post_init(:1211-1228) finalize_relocations→ICache::invalidate_range→register_nmethod→CodeCache::commit；依赖登记 nmethod.cpp:1149-1160——CallSite 挂实例(:1153)/类依赖挂 InstanceKlass(:1160)，DependencyContext::add_dependent_nmethod(dependencyContext.cpp:92) 头插+CAS 免扫描（不变量：新 nmethod 要么不在链、要么在表头），类加载只查相关类→线性变常数；Method::set_code(method.cpp:1441-1464) 写序 _code(:1451)→storestore(:1460)→_from_compiled_entry=verified_entry_point(:1461)，解释器 from_interpreted_entry→i2c→_from_compiled_entry 衔接 03 站 jump_from_interpreted；状态机 nmethod.hpp:690-694 not_installed=-1/in_use=0/not_entrant=1，make_not_entrant(:2215) 幂等(:2225)→OSR invalidate_osr_method(:2249)/常规 BarrierSetNMethod 入口 patch(:2253)→inc_decompile_count(:2258 正是 04 站 too_many_traps 的输入)→try_transition(not_entrant)(:2270)→unlink_from_method(:2277)；回收 unlink(:2293 flush_dependencies :2299)→purge(:2320 物理释放)。
- 验证（verify05-codegen.js）：20 pills；20 步 desc/stage/src 三层断言 + scene 断言 + 每步计数器格式（第 i / 19 步）+ 连点冒烟 + 四关卡导航（step4→关卡1/step5→关卡2/step9→关卡3/step15→关卡4）全绿。
- 修复记录：首跑 2 处断言词不匹配——step3 的 RAW 在 stage（"不破坏数据依赖（RAW/WAR/WAW）是底线"）不在 desc，移入 stage 断言；step16 desc 实际文案"线性变成常数"（断言写成了"线性变常数"，stage 里才是缩写）——断言从实际文案抄。
- 经验：① 05 站 CSS 给 .stageBox 补了 white-space:pre（04 站没加，多行舞台 ASCII 图会被 HTML 折叠成一行）——系列后续动画应沿用；② 断言关键词"从实际文案抄"原则再次应验，中文文案别缩写，英文标识符按源码原样。
- 五站闭环完成：「一条 invokevirtual 的一生」——01 linkResolver 符号解析 → 02 klassVtable 分派表 → 03 templateTable 解释器快路径 → 04 doCall/bytecodeInfo C2 去虚化+内联 → 05 Code_Gen 编译落地 + 依赖失效/去优化回收；解释器兜底贯穿始终。
- 用户反馈：五个交互动画术语门槛过高「根本看不懂」，要求通俗解释。已用「老板喊人干活」比喻体系白话串讲五站（invokevirtual=喊岗位名；解析=查名册记小本；vtable=员工名册；解释器=老实人翻名册+画正字；JIT/去虚化/内联=配速算卡；nmethod=速算卡入抽屉；依赖=卡上假设「会计=老王」；make_not_entrant=贴停用条；OSR=算一半无缝换人）。约定：后续文档/动画若再加工，白话入口应前置（先人话后源码）。

## 五站动画插入「白话速览」step0 + 五个验证脚本全绿（08-21 续）
- 用户批准「把白话速览做进动画」：五站各插 1 步白话速览作为新 step0（faces(1)、fnote='白话速览<br>先讲人话'、stage 蓝字比喻、src 术语对照表、scene='白话速览：<b>…</b>'），比喻体系与速查表一致（01 第一次喊「会计」/02 员工名册/03 老实人照说明书+画正字/04 配速算卡+赌输撕卡/05 速算卡生产车间+仓库+回收站）。原有关卡边界索引不动（新 step0 归关卡①），整体步骤数 +1：01 16→17、02 16→17、03 17→18、04 19→20、05 20→21。
- 顺手统一：01-04 补 `.stageBox{white-space:pre}`（05 上次会话已有），系列 5 站一致，避免多行舞台 ASCII 图被折叠成一行。
- 验证脚本同步更新（verify01-05）：
  - verify01：17 pills、首项断言（白话速览/第一次喊/一次解析 + scene 检查）、冒烟 `17/17` 停 pill 17；模式=历史不 reset 顺序点 next。
  - verify02：17 pills、首项含 override 术语对照；verify03：18 pills、冒烟 scene `17 / 17`；verify04：20 步、计数器 `第 i / 19 步`、faceChecks [[5,2],[10,3],[16,4]]；verify05：21 步、计数器 `第 i / 20 步`、faceChecks [[5,1],[6,2],[10,3],[16,4]]。**faceChecks 与计数器是插速览步后最容易漏改处。**
- 修复记录：verify01 首跑 2 处断言词与 HTML 实际文案不符（步10 desc 是「权限」不是「访问检查」；步15 src 是 `vtable_call : CallInfo::direct_call` 非 `vtable_call : direct_call`），从实际文案抄后全绿。
- **重要工具坑：Write 工具在 Windows 上会把中文内容写成 GBK 编码**（verify01 用 Write 整体重写后 278 个 U+FFFD，UTF-8 读全是乱码；verify02-05 是 Edit 修改既有 UTF-8 文件所以正常）。修复：Python 读 GBK → decode('gbk') → encode('utf-8') 写回（字节内容本就是正确字符，只是编码错）。**经验：Windows 下用 Write 写含中文的 .js 文件后，必须先 `node -e "fs.readFileSync(f,'utf8').includes('中文')"` 验证编码；优先用 Edit 改既有 UTF-8 文件。**
- 验证结果：verify01-05 全部 ✅ 全绿（01: 17/17 步 + smoke pill 17 + 零 jsdom 错误；02/03 之前已绿；04: 20 步 + 冒烟 + faceChecks 全过；05: 21 步 + 冒烟 + faceChecks 全过）。

## 五站动画每步插入「人话」引导 + 验证脚本加徽章断言全绿（08-21 续）
- 用户反馈「动画里面的每一步的解释也要优化，看不懂」——仅首步白话速览不够，要求**每小步都先讲人话、再上源码**。沿用既定约定「先人话后源码」，不重做动画、不破坏关卡导航/源码行号/验证脚本原有断言。
- 方案：每步 d 文案最前面插入「人话徽章 + 这一步在干嘛」引导句（step0 白话速览除外）：
  - 徽章样式（黄色系，与蓝色白话速览区分）：`<span style="display:inline-block;background:#fef3c7;color:#92400e;border-radius:999px;padding:2px 10px;font-size:12px;font-weight:500">人话</span> <b>这一步在干嘛</b>：…`
  - 共 88 句（01=16、02=16、03=17、04=19、05=20），全部沿用「老板喊人干活 / 名册 / 速算卡」比喻体系并与各步技术点一一对应（如 04 步13「档案说几乎总是 Dog → 按 Dog 赌一把」=profile 判断；05 步18「先写 _code → 内存屏障 → 最后写 _from_compiled_entry」=set_code 三步序）。
- 批量插入机制：Bash heredoc + Python 显式 `open(path,'w',encoding='utf-8')`（**规避 Write 工具 GBK 坑**）；`re.finditer(r'\{ d:', s)` 定位每步起点 → `s.index('`', pos)` 找 d 模板串起始反引号 → 反引号后插入；断言 `len(idxs) == len(sentences) + 1` 防步骤数不匹配。插入后校验：徽章数 01=16/04=19/05=20，UTF-8 干净，sed 抽查 01 步1 插入位置正确。
- 验证脚本同步加「人话徽章」断言（step0 白话速览必须**不含**、其余每步必须**含**「这一步在干嘛」）：
  - verify01/02：`const problems=[]` 后加 `if (i>0 && !descText.includes('这一步在干嘛')) …` + step0 反向断言。
  - verify03：`renHuaOk = (i===0) ? !descTxt.includes(…) : descTxt.includes(…)` 并入 `ok` 条件 + 失败输出分支。
  - verify04/05：`badRen = (i>0 && !d.includes(…)) || (i===0 && d.includes(…))` 并入失败条件 + 输出分支。
- 运行结果：verify01-05 全部 ✅ 全绿（01: 17 步含 16 个人话徽章前缀、smoke pill 17；02: 17 步；03: 18 步 + 冒烟 17/17；04: 20 步 + 冒烟 + 关卡；05: 21 步 + 冒烟 + 关卡；零 jsdom 错误）。
- 经验：① 验证脚本加「新 UI 元素存在性」断言时，step0 与其余步要分正反两个方向（step0 断言不含、i>0 断言含），防止徽章误插进速览步或漏插；② 动画文案加工用 Python heredoc 批量插入比逐个 Edit 更可控（有断言防步骤数漂移）；③ 断言关键词仍从实际文案抄（如「这一步在干嘛」文案格式统一后再断言）。

## 01 站 step1「4 道关卡总览」下沉到源码层（08-21 续）
- 用户批评 step1 总览「写的过于简单」——原文每关只给"函数名 + 行号"一句话（①resolve_invoke 按字节码类型分路 :1715 ②resolve_method 七步 :753 ③按接收者实际类型 :1412 ④写回 ConstantPoolCache :138）。
- 本地源码核实（OpenJDK 28 mainline）：
  - 解释器入口 `InterpreterRuntime::resolve_invoke`（interpreterRuntime.cpp:864）：取当前帧方法+bci 构造 Bytecode_invoke(:872-873) → virtual/interface/special 提取接收者 :869-875（callee_receiver）→ `method_index = last_frame.get_index_u2(bytecode)` :890 → `LinkResolver::resolve_invoke(info, receiver, pool, method_index, bytecode, init_preemptable)` :893-895。
  - `LinkResolver::resolve_invoke`（linkResolver.cpp:1715）：`switch(byte)` 六条管线 :1717-1722（static/special/virtual/handle/indy/interface），每条 CHECK 错误传播；各 resolve_xxx 先构造 `LinkInfo(pool, index, bytecode)` 再调 resolve_xxx_call（如 resolve_invokevirtual :1770-1777）。
  - `resolve_method` :753、`runtime_resolve_virtual_method` :1412、`CallInfo::set_common` :138 行号全部属实。
- 深化后 step1 d 文案：补「为什么只有第一次」（ConstantPoolCache 槽检查）、「怎么走进来」（invoke 模板 → interpreterRuntime.cpp:864 → 取 index+receiver → linkResolver.cpp:1715），每关补 1-2 行真实机理（关卡① switch 六管线+CHECK；② 类链+接口链查符号；③ 实际类型查 vtable/itable 出 selected_method；④ set_common 写回两种方法+调用形态），收尾点明"静态绑定 vs 动态分派"决定后续走向。stage/src/scene 未动（最小改动）。
- 验证：verify01 加固定锚点断言 `i===1 && !descText.includes('interpreterRuntime.cpp:864') → FAIL`（防总览退化回一句话）；17 步全绿 + smoke pill 17 + 零 jsdom 错误。UTF-8 抽查正常。
- 经验：批评"过于简单"的文案，深化时优先补**调用链**（谁调谁、行号、关键参数）与**机制**（为什么、做了什么），而非只加形容词；把关键锚点固化为断言防退化。
- 用户再次反馈：深化后的 step1 仍看不懂，明确指向两个概念——「ConstantPoolCache 这是什么」与「invoke 模板→InterpreterRuntime::resolve_invoke→LinkResolver::resolve_invoke 这又是什么」，要求"要么画图，要么解释清楚"。教训：**堆术语+行号 ≠ 讲清楚；概念本身要先白话+可视化**。
- 本轮源码核实（JDK 28 mainline 重要差异）：模板解释器未解析分支 call 的入口是 `InterpreterRuntime::resolve_from_cache`（interpreterRuntime.cpp:1063，switch 按字节码分路：get/put→resolve_get_put、invoke 类→resolve_invoke、handle/indy 各自）→ invoke 类才转 `resolve_invoke` :864；模板侧"已解析？"判断在 `resolve_cache_and_index_for_method`（templateTable_x86.cpp:2301）：`load_method_entry` :2313 读槽 → `cmpl(temp, code)` 比对 bytecode1/2_offset :2324 → 不等才 `call_VM_preemptable(..., resolve_from_cache, ...)` :2343-2345。缓存槽是新的 `ResolvedMethodEntry`（resolvedMethodEntry.hpp:68：_method/_table_index/_cpool_index/_number_of_parameters/_tos_state/_flags[_bytecode1,2]），替代老 ConstantPoolCacheEntry。官方注释 cpCache.hpp:46-49 可作"速查本"定义的依据。
- 交付：① 对话内两张 SVG（图1 档案柜 ConstantPool vs 速查本 ConstantPoolCache 结构图；图2 invoke 第一次执行全旅程，fast/slow 双路径+行号）；② 动画 step1 两处黑话白话化（速查本比喻 + 完整调用链 resolve_from_cache:1063→resolve_invoke:864→取 index:890/接收者:869→LinkResolver:893）；③ verify01 锚点断言升级为双锚点 `interpreterRuntime.cpp:1063 && :864`；17 步全绿 + smoke + 零 JS 报错。
- 经验：写调用链时必须写**完整入口**（模板 call 的是 resolve_from_cache 而非 resolve_invoke，旧版 JDK 才是直接 resolve_invoke）——行号断言要按当前源码树的真实路径来，别照记忆写。

## 01 站插入「method_at_vtable 拆解」步骤 + verify01 重建全绿（08-21 续）
- 用户问「一大堆 method_at_vtable 什么意思」→ 先在对话里白话+源码讲清（method_at_vtable=按号取人，vtableEntry 单字段 Method* _method klassVtable.hpp:195，实现 start_of_vtable()[index].method() klass.cpp:1125 一行数组下标），随后用户要求「你在动画里解释呀」→ 直接做进动画。
- 改动：01-linkresolver-animation.html 在 runtime 分派(index 12)与 vtable 多态之间插入新步骤（index 13，注释 `// 13 method_at_vtable 拆解`，faces(3)，scene「场景 12：method_at_vtable 拆解 · 按号取人」）——d 拆名字/格子/实现/号与名册谁定/多态本质，stage 一行核心+vtableEntry 结构，src 引 klass.cpp:1118-1126 + klassVtable.hpp:182-202 原文；原 vtable 多态 d 开头加承接句「上一步拆了构造，这一步看翻名册翻出什么」。总步数 17→18，后续 scene 编号全部 +1（场景 13/14/15/16），注释编号同步（//14//15//16//17）。
- verify01.js 重建（历史脚本未落盘，按日志模式重建）：18 pills、step0 白话速览正反断言、i>0 人话徽章、step1 双锚点 interpreterRuntime.cpp:1063&&:864、新步骤三层锚点（desc 按号取人/start_of_vtable，stage start_of_vtable()[index].method()，src klass.cpp:1118-1126+klassVtable.hpp:182-202+Method* _method）、scene 顺延断言、faceChecks [[4,2],[12,3],[16,4]]、冒烟 18/18、零 jsdom 错误——全部 ALL GREEN。
- 经验：① 插入步骤后 scene 编号=index-1（白话速览占 index 0）的规则要整体顺延，注释编号与 scene 编号分开核对；② verify 脚本未落盘，每次会话后重建成本高——后续考虑把 verify0X.js 持久化到 .workbuddy/docs/method-call/ 目录随动画一起管理（本次已落盘 verify01.js 在动画同目录）。

## 常量池 2 站 + 类加载 3 站双线收官 + README 归档（08-21 晚）
- 用户要求「常量池 类加载 读源码输出文档和交互动画」→ AskUserQuestion 对齐：常量池 2 站（①类文件常量池 #12 长什么样 ②运行时常量池与解析）+ 类加载 3 站（①加载与双亲委派 ②链接 verify/prepare/resolve ③初始化与卸载），两线同时开工。
- 交付物（每站三件套：md + 手绘 SVG + 15 步交互动画）：
  - constantpool/01-classfile-cp.md + overview.svg + animation.html（15 步，tag 速查/Dog 示例拆解/parse_constant_pool）
  - constantpool/02-runtime-cp.md + overview.svg + animation.html（15 步，懒解析/ConstantPoolCache/双胞胎）
  - classload/01-load-parent-delegation.md + overview.svg + animation.html（15 步，loadClass :501/:546 → JVM_DefineClass → SystemDictionary :606 → PlaceholderTable :225/:255 → ClassLoader::load_class :1100 → KlassFactory :172 → create_instance_klass :5304）
  - classload/02-link-verify.md + overview.svg + animation.html（15 步，parse_stream :6024 → Verifier::verify :183 → link_class_impl :1123 → rewrite :1232 → vtable/itable :1256 → prepare :522 → 懒解析）
  - classload/03-init-unload.md + overview.svg + animation.html（15 步，_new :223 / 静态字段 :741 触发 → initialize :961 → initialize_impl :1417（init_lock :1436 / wait :1442 / 递归 :1454 / being :1498）→ 父类先行 :1539 → call_class_initializer :2019 → fully_initialized :1620 / 错误表 :1341 / ExceptionInInitializerError :1645 → do_unloading :410 整批回收）
- 关键源码锚点（agent 实测，JDK 28 mainline）：类加载站① Java 层 loadClass(String) :501 → loadClass(String,boolean) :546（:549 加锁 / :551 findLoadedClass / :556 parent / :558 findBootstrapClassOrNull / :569 findClass），native defineClass1 :1088/:1093/:1110 留在 ClassLoader.java（无独立 JVM.java）；JVM_DefineClass jvm.cpp:1240 → jvm_define_class_common :1073（ClassFileStream :1098 / resolve_from_stream :1102）；SystemDictionary::resolve_instance_class_or_null systemDictionary.cpp:606（register_loader :618 / ObjectLocker :635 / PlaceholderTable::get_entry :654 / find_and_add :705 / load_instance_class :728 / find_and_remove :736），load_instance_class_impl :1363（CDS :1430-1440 / ClassLoader::load_class :1445）；ClassLoader::load_class classLoader.cpp:1100（jrt open_stream :1160 / KlassFactory :1196）；KlassFactory::create_from_stream klassFactory.cpp:172（ClassFileParser :200 / create_instance_klass :208，定义 classFileParser.cpp:5304）；placeholders.cpp get_entry :225 / find_and_add :255 / DETECT_CIRCULARITY :267-268 / circularity_detection_in_progress :655 / ClassCircularityError :718。
- 站②锚点：parse_stream classFileParser.cpp:6024（guarantee_more(8, CHECK) :6031 / 魔数 :6033-6036 / 版本 :6039-6040 / verify_class_version :6043 / ConstantPool::allocate :6058 / parse_constant_pool :6064）；verify_code instanceKlass.cpp:1063 → Verifier::verify verifier.cpp:183（is_eligible_for_verification :199 / split ClassVerifier :222-225 / inference 回退 :248）；link_class :1106（is_linked 防重入）/ link_class_or_fail :1115 / link_class_impl :1123（父类递归 :1150-1166 / 接口 :1168-1174 / init_lock :1200-1205 / verify_code :1218 / rewrite_class :1232 / link_methods :1238 / vtable+itable :1256-1257 / set_init_state(linked) :1271/:1277 / JVMTI :1279-1281）；rewrite_class :1296（Rewriter::rewrite :1302）；link_methods :1309（Method::link_method :1317）；vtable klassVtable.cpp:615 / itable :1223；prepare allocate_instance_klass instanceKlass.cpp:522（static_field_size :523-527，classFileParser.cpp:5194）；懒解析 klass_at_impl constantPool.cpp:631 / resolve_invoke interpreterRuntime.cpp:864 / LinkResolver::resolve_invoke linkResolver.cpp:1715——链接期不集中解析。
- 站③锚点：_new interpreterRuntime.cpp:223（klass_at :224 / check_valid_for_instantiation :228 / initialize_preemptable :231 / allocate_instance :233）；静态字段 uninitialized_static :741；initialize instanceKlass.cpp:961（should_be_initialized :962）；initialize_impl :1417（link_class :1422 / ObjectLocker :1436 / wait_uninterruptibly :1442-1450 / 递归放行 is_reentrant :1454-1462 / 已完成 :1465-1473 / error→NoClassDefFoundError :1476-1494 / set_init_state(being) :1498 / set_init_thread :1499）；状态机四态 instanceKlass.hpp:614-618；父类先行 :1536-1546（super_klass->initialize :1539 / initialize_super_interfaces :1546 / 失败 :1550-1562）；call_class_initializer :1578 / class_initializer :2010（find_method :2011）/ JavaCalls::call :2057；成功 fully_initialized :1618-1620 / 清 _init_thread :1658 / notify_all :1661；失败 add_initialization_error :1633 / InitializationErrorTable :1341 / get_initialization_error :1377 / THROW_ARG ExceptionInInitializerError :1645；卸载 do_unloading classLoaderDataGraph.cpp:410 / ClassLoaderData::unload classLoaderData.cpp:604 / is_alive :693 / SystemDictionaryShared::handle_class_unloading :947。
- 生成管线：gen-cl1/cl2/cl3.py 统一 JSON 序列化方案（json.dumps 生成步骤纯数据，JS 端 steps.forEach 挂 fn，绕开 f-string 转义；CSS 共用 cl1-css.txt 的 blu/org/grn/pur/red/dim 类）；verify-cl1-load/cl2-link/cl3-init.js 三层断言 + 人话徽章双向断言（step0 不含「这一步在干嘛」、i>0 必含）+ faceChecks + 连点冒烟，全部 ALL GREEN。站③修复两处断言差异（while 行范围 :1442-1450 非独立 :1450；is_in_error_state()) { 双括号），另清除一次编辑残留的重复 scene 行。
- README 归档：docs/README.md 目录表追加 constantpool/ 与 classload/ 两行（✅ 2 篇 / ✅ 3 篇收官）、速览两节、交互动画表 +5 行、「双亲委派」从待跟进勾选为已纳入；补 constantpool/README.md 与 classload/README.md 两个子 README（含三站时序衔接 ASCII 图）。
- 经验：① 系列收官时子 README（object/、keywords/ 先例）必须同步补，主 README 索引链接才不悬空；② verify 脚本编辑遗留的重复行会在语法检查阶段暴露（Unexpected token），修复后再跑断言；③ 断言与文案的差异多为「范围写法 vs 独立行号」「括号细节」，HTML 本身往往正确，先读实际文案再改断言。
